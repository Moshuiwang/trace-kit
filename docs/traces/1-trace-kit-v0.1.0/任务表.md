# 任务表（状态即文件当前值，编排者随执行 commit 更新；第一恢复点）

> 执行路由：`claude_tmux_named_session`，session `TZ-server`，窗口 `trace-kit-b1`（单批）。编排 Fable 5.1 max；实施 Fable 5.1 xhigh（机械改动 opus high）；唯一审核 Fable 5.1 xhigh。证据只认 GitHub / 仓库留痕；每个 Step 完成时勾选并附指针。

## W0 只读盘点

- [x] S-0a 克隆 + 推送冒烟（`bootstrap` 空提交推送后删除）— 通过，App 对本仓有写权
- [x] S-0b Trace Issue #1 + 第 0 步 G1/G2/G3 分级清单 — [评论](https://github.com/Moshuiwang/trace-kit/issues/1#issuecomment-5503411104)
- [x] S-1 三件套 + `METHOD.md`（#147 v16 原样 + 版本头）入库 — PR #2 合并（main `8b27e68`）= 合同 v1 批准

## W1 并行打包（各自 worktree + 分支，PR 串行合 main；每目录带 README「是什么 / 怎么用 / 换什么」）

> 2026-09-02 03:3x UTC / 11:3x 北京：S-2a–S-2e 五个实施子代理同时派出（分支 `pack/{plugin,tdocs,tci,tdeploy,examples}`），编排者兜底观察 45 分钟。

- [ ] S-2a `plugin/`：五 skill（kickoff / takeover / handoff / guardian / dispatch-card）+ `templates/`（合同 / 任务表 / 验收 / 派发卡）+ `.claude-plugin/plugin.json` + 根 `marketplace.json`；`claude plugin validate --strict` 绿
- [ ] S-2b `template/` 文档与 GitHub 约定：AGENTS.md / CLAUDE.md / docs 骨架 / Issue & PR 模板 / CHANGELOG 约定
- [ ] S-2c `template/` 分层 CI + `scripts/ci` 通用检查 + `scripts/dev/check.sh`；对 template 自身全绿
- [ ] S-2d `template/deploy/`：compose profile + 不可变 tag + digest 固定 + runbook / 预检清单骨架 + 监控告警到群；`check_deploy_contract.py` 精简版 + `verify_compose_structure.sh`
- [ ] S-2e `examples/lingxi/README.md`：G3 清单（链接到 lingxi 固定 SHA），不复制正文

## W2 集成与自验证

- [ ] S-3 集成：`init.sh`、`scripts/kit/{smoke.sh,check_no_lingxi.sh,refill_diff.sh}`、本仓 `kit-selfcheck` 工作流、根 README / CHANGELOG 草稿
- [ ] S-4a 自验证①自回灌：diff 报告落 `验收.md`
- [ ] S-4b 自验证②空项目冒烟：check.sh 三层 + scripts/ci 全绿 + plugin validate + kickoff→三件套→勾选
- [ ] S-5 唯一审核者批量审查（只读）→ 统一修复包 → 同一审核者定向复核

## W3 发布与收口

- [ ] S-6a `v0.1.0` tag + CHANGELOG（每资产出处 + 日落条款）+ README（安装两步）+ Template 开关尝试
- [ ] S-6b 收口评论（UTC+北京、子代理人次、审核轮次、证据等级、未验证清单）+ 环境盘点回读（worktree / 远端分支 / 临时目录清零）
