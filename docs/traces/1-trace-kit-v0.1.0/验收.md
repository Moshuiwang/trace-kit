# 验收（可观察完成标准 + 证据等级目标 / 现值；证据落 Issue #1 或 PR）

> 证据等级口径：产品负责人全局 1–7 级（1 已分析 / 2 已改代码 / 3 本地测试通过 / 4 CI 通过 / 5 已部署 / 6 真实旅程验证 / 7 回源确认）。本 Trace 无部署面；「真实旅程」= 用插件 skill 在空项目走通 kickoff→三件套→勾选。

## 逐项关卡

| 项 | 可观察完成标准 | 目标等级 | 现值 | 证据 |
| --- | --- | --- | --- | --- |
| METHOD.md | 与 #147 v16 正文逐字一致（`diff` 除版本头外零差异） | 3 | **3** | 本机 `diff <(gh issue view 147 -q .body) <(tail -n +5 METHOD.md)` 零差异（2026-09-02 08:3x UTC） |
| plugin/ | `claude plugin validate --strict` 通过；五 skill 各有 frontmatter；模板文件被 skill 引用的路径真实存在；无 lingxi 禁词 | 3 | **3** | PR #3；集成分支复跑 validate 双绿、`--plugin-dir` 加载冒烟列出五 skill；禁词零命中 |
| template/ 文档 | `verify_docs.sh` 在空项目上通过；每目录 README 三问齐；无禁词 | 3 / 4（kit CI） | **3** | PR #5；冒烟第 1 步 `verify_docs.sh` 全部通过（kit CI 待 PR #8） |
| template/ CI 与脚本 | 空项目上 `check.sh docs/fast/full` 与全部 `scripts/ci/*` 通过；工作流 YAML 可解析、`gate_spec.py` 能现读版本 | 3 / 4（脚本）；3（工作流，未上真实 Actions） | **3** | PR #6（79 条用例、docker build）；冒烟第 2 / 5 步三层全绿、4 份工作流解析 |
| template/deploy | `check_deploy_contract.py` 与 `verify_compose_structure.sh`（有 docker 时）对骨架通过 | 3 | **3** | PR #7（改坏验红、告警闭环）；冒烟第 3 / 4 步通过（docker compose v2.40） |
| examples/lingxi | 每条链接指向 lingxi 固定 SHA 且可达 | 3 | **3** | PR #4：34 条路径 `cat-file -e` 全存在、24 个 Issue 号标题核实 |
| 自回灌 | diff 报告：lingxi 侧差异行全部命中 G3 关键词或人工复核为 G3 | 3 | **3** | [自回灌报告.md](自回灌报告.md)：文档侧 ~100 行未命中行逐一复核为 G3 / 改写，六处疑似误裁 grep 确认仍在 |
| 空项目冒烟 | 上述全绿 + kickoff→三件套→任务表勾选一条 | 6（真实旅程，本机） | **6**（本机真实旅程） | `scripts/kit/smoke.sh` 六步全绿（init.sh → verify_docs → check.sh 三层 → 部署契约 → compose 结构 → 工作流解析）；kickoff 真实旅程见下方追记 |
| 独立审查 | 一次批量审查零 P0/P1 未闭；P2 逐条去向 | 3 + 独立复查 | **3 + 独立复查** | 批量审查（候选 `dd53ecf`）：0 P0 / 4 P1 / 10 P2，8 处变异实测红→绿；修复包 PR #9；定向复核（`b5bb404`）P1-1～P1-4、P2-5～P2-13 全部闭合；N1–N3（P2）随发布 PR 修 |
| 发布 | `v0.1.0` tag、CHANGELOG 每资产出处链接、README 安装两步；Template 开关结果留痕 | 4 | — | — |

## 未验证层级（收口时如实列）

- template 工作流在真实 GitHub Actions 上的运行（需第一个真实试穿项目）。
- ~~插件通过 `claude plugin install trace-kit@trace-kit` 从远端 marketplace 安装~~ 已验证（2026-09-02 09:0x UTC：`marketplace add Moshuiwang/trace-kit` → `install` → `plugin list` 显示 0.1.0 enabled → 卸载还原）。
- 仓库「Template repository」开关：机器人 `gh repo edit --template` 返回 403（App 无仓库管理权限），需产品负责人在 Settings → General 勾选；勾选前安装第一步用 README 写明的 `git clone` 替代法。

## 追记：空项目冒烟与 kickoff 真实旅程（2026-09-02 08:2x UTC / 16:2x 北京）

- **机械部分**：`scripts/kit/smoke.sh` 在本机临时目录起空项目（拷贝套件 → `init.sh` → 提交），六步全绿：`verify_docs.sh`；`check.sh docs / fast / full`（venv 走 `uv` 退回，79 条测试）；`check_deploy_contract.py`；`verify_compose_structure.sh`（docker compose v2.40，stage / prod 结构一致、生产零 build）；四份工作流 YAML 解析（ci 5 jobs / docs 1 / publish 2 / story 5）；门禁未改写工作树。
- **真实旅程**：在该空项目里 `claude --plugin-dir <套件>/plugin --model opus --permission-mode acceptEdits -p "用 /trace-kit:kickoff …"`——插件 skill 读取 `templates/` 生成 `docs/traces/1-demo/{合同,任务表,验收,tracking-issue}.md`（91 / 23 / 39 行 + 瘦指针），任务表 S-0 已勾选 `[x]`，合同含六段、合同区与「批准时需一并裁定」清单，方法版本行正确指向 `docs/协作/执行方法.md`；`git commit` 因非交互会话无 Bash 权限未执行，由编排者以空项目身份提交（`071bc3e`），随后 `scripts/ci/verify_docs.sh` 对含演示三件套的仓库**全部通过**（链接 / 矩阵 / 棘轮 / 归属 / 体量 24088B）。
- 未验证：`claude plugin install trace-kit@trace-kit` 远端安装路径（发布后补）；template 工作流在真实 GitHub Actions 上运行（套件自身 `kit-selfcheck` 在 PR #8 起跑，其中包含本冒烟）。

