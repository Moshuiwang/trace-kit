# 任务表（看板 v1 Trace #17；状态即文件当前值，编排者随执行 commit 更新；第一恢复点）

> v1（2026-09-05 14:0x 北京）。执行路由：`claude_tmux_named_session`，session 常驻 session（名字见接手卡）：**`hb-b1`**（Fable 5.1 max，auto）；元守护＝lingxi 规划者会话（窗口 `guardian`）。代理只改复选框与括号内指针，不改 Step 文字。裁定终值见 [决策清单.md](决策清单.md)，关卡见 [验收.md](验收.md)。
> 纪律：唯一 Draft PR（`batch/17-board → main`）；kit-selfcheck ≤ 10 次；三件套回写随分支 commit；进度写 #17 评论；**不碰 `~/project/lingxi` 主工作树**。

## 块 A

- [x] A-1 块 A 六项（A-1…A-6）已给齐（2026-09-05 13:4x–13:5x 北京；终值见决策清单）

## Wave 0

- [x] W0-1 接管登记＋只读盘点（main `300ded8`、样稿分支、插件安装现状、`ux-board.py` 现状、tmux 窗口、codex 一次真实调用、G-2 分类器判定、gh 限额）→ #17 接管评论 —— 已落 [评论 5549924168](https://github.com/Moshuiwang/trace-kit/issues/17#issuecomment-5549924168)（2026-09-05 14:17 北京）：main 实为 `9d91a38`（三件套自身合入）、插件未装、G-2＝docs 路由、codex 13 s 真实返回；接口约定＋骨架 `df21101` / `ac1c620`
- [x] W0-2 简易版真实快照（#606 数据；四档边框样张；三行模块；角标）贴 #17 → PM 单字（24h 默认批）—— 原型（worktree `5cbe609`，Fable xhigh，13 分钟）三份快照收进 `快照/w0-2-*.txt`，已贴 [评论 5550183045](https://github.com/Moshuiwang/trace-kit/issues/17#issuecomment-5550183045)（2026-09-05 15:10 北京；24 h 时钟到 09-06 15:10）；真实发现：#606 评论首行无 Step ID → 轮数归属回落时间窗（决策清单 C-7）；**PM 单字待回**

## Wave 1（实施；各自 worktree，改完先 commit 再汇报）

> 2026-09-05 14:33 北京：W0-2 / S-1 / S-2 / S-3 / S-4 五个实施子代理同时派出（各自 `~/project/trace-kit-wt/<step>`，分支 `wt/17-<step>`；独立 `claude -p` 进程按合同档位 Fable xhigh ×4、Opus high ×1），编排者挂进程判活＋提交落地双哨。

- [x] S-1 #13 七项＋CODEOWNERS＋CHANGELOG 出处（Opus high）—— 合入 `batch/17-board`（worktree 提交 `91905dc` / `e2bf520`；2026-09-05 14:4x 北京）：合同模板 6336 → 6137 B（关卡 5 达成，触发条件搬进 kickoff skill，现场裁定 C-6）；禁词 / 链接 / 空白 / plugin validate 双绿 / smoke --strict 6/6 由子代理跑、编排者独立复核前四项；空项目 kickoff 真实旅程留 S-7a
- [ ] S-2 解析＋模块分组＋证据登记表＋状态推断（九色＋未知、五级阶段、轮数、时长）（Fable xhigh）
- [ ] S-3 渲染：简易 / 复杂视图、`v` 切换、四档边框＋图例、来源角标、时长、键盘逐序列、差异重画、看门狗、整轮超时、`--dump --why`（Fable xhigh）
- [x] S-4 证据源配置 schema＋通用 provider＋lingxi 配置草案（只读试跑一次）（Fable xhigh）—— 合入 `bc637b9`（worktree `e517d8e` / `77ac896`；37 例单测；gh 三条预算命令已试跑 2 / 0 / 1）；**lingxi 三条 ssh 只读试跑被分类器拦截＋PM 拒绝 → 记「未知」不绕，R-6 处置待 PM 单字（甲 / 乙 / 丙）**；草案在本机归档目录 `board.lingxi.toml`
- [ ] S-5 夹具（回放 / 简单三时刻 / 复杂四故障 / 未知两种 / 五级三时刻 / 键盘字节流）＋接入 kit-selfcheck（Opus high）
- [ ] S-6 skill `/trace-kit:board`、README、CHANGELOG、版本 0.2.0（Opus high）

## Wave 2（集成、批终链）

- [ ] S-7a 集成 → `smoke.sh --strict`＋`plugin validate --strict` 绿 → 推送 → Draft PR → kit-selfcheck 绿
- [ ] S-7b Fable 审①＋codex 外审 ×2（状态推断 / 渲染与并发）→ 唯一缺陷账本落 #17
- [ ] S-7c 修复包 → 复核② → 推送 → 绿
- [ ] S-7d K-9 条件落 #17 → `gh pr merge --merge` → tag v0.2.0 → CHANGELOG 版本段

## Wave 3（lingxi 试穿与收口 PR）

- [ ] S-8a tz 安装 / 更新插件（D-4）→ 新窗口 `看板-v1` 跑 #606 与旧 `看板` 并排 ≥ 1 h → 两份 `--dump` 对照落 #17
- [ ] S-8b PM 在 tmux 看一次 #606（收口验收）→ 单字
- [ ] S-8c 换 `看板` 窗口为新脚本；`ux-board.py` / `rc25-board.py` 退役留档（`lingxi-archive/README` 一行）
- [ ] S-8d lingxi 收口小 PR（`docs/traces/board.toml`＋README 一行；R-3 时机由元守护裁）→ 合入
- [ ] S-8e lingxi #577 #578 #579 #580 #581 #582 #589 关闭评论各带指针

## 收口

- [ ] S-9a 环境盘点：trace-kit worktree / 远端 `batch/17-board` / tmux 窗口 / 临时目录 / lingxi worktree
- [ ] S-9b 任务表勾选回写；验收现值回填
- [ ] S-9c 收口评论（UTC＋北京、kit-selfcheck 次数 / PR 数 / commit 数 / 人次 / 审核轮次 / 证据等级 / 未验证清单）→ #17 关闭 → lingxi #547 看板（元守护）→ 记忆
