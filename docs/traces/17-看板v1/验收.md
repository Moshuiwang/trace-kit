# 验收（看板 v1 Trace #17；关卡＝#12 v3 ＋ #13 ＋ lingxi 七张 Issue 完成标准；证据落 #17 / PR）

> v1（2026-09-05）。证据等级目标：脚本与夹具 3（本机）→ 4（kit-selfcheck）；#606 真实试穿 6（本机真实数据）；双通道＝看板数字 / 状态 ↔ `git` / `gh` / 镜像 tag 回读一致。

## 总关卡

| # | 关卡 | 证据 | 等级 | 现值 |
| --- | --- | --- | --- | --- |
| T-1 | #13 五条：smoke `--strict` 绿、plugin validate 双绿、kit-selfcheck 绿；空项目 kickoff 真实旅程一次（合同无空表、上限为数字、含「未知＋Owner＋时点」）；派发卡刹车句、kickoff 依赖自检句、任务表只改状态位句；CODEOWNERS 存在且 `check_links.py` 通过（规则集一条标「需 PM」）；CHANGELOG 每条出处、合同模板字节数下降 | PR＋#17 评论 | 4 | — |
| T-2 | 夹具全绿并进 `kit-selfcheck`：Trace #1 回放三件事（299 分钟空档 / 24m36s 等待 / 九 PR 自合）；简单三时刻；复杂四故障；**未知两种**（gh 不可用、外部命令失败 → 显示「未知」不回落）；**五级阶段三时刻**（合主干 / 发布 / Issue 关闭）；**键盘字节流**（十连按、半序列拼接）；线程异常后仍能刷新；整轮超时 | CI run | 4 | — |
| T-3 | #606 真实试穿：简易版 150×52 不滚动、四档边框可辨（PM 单字）、每模块三行与「实际或已跑 / 预估」、来源角标；随 ux-b1 推进，合 main / Main Publish / stage 升级 / 生产升级 / #606 关闭各时刻各留一份 `--dump`，阶段在一轮刷新内变化；`--dump --why` 每个状态可指出证据 | #17 评论（快照） | 6 | — |
| T-4 | #579 五处失真复现不了（夹具重放当时证据）；状态 → 证据登记表与文档同源，新增状态未登记即单测红 | PR | 3 | — |
| T-5 | #580 三条：连按十次移十行；长按连续；半序列跨读 | 夹具＋tmux 实测 | 3–6 | — |
| T-6 | #582 干净目录安装：tz 新临时目录 clone trace-kit @ tag → 装插件 → `--dump` 出 #606 快照；节点结构断言（错状态字符串即报错） | #17 评论 | 4 | — |
| T-7 | lingxi 收口：`docs/traces/board.toml`＋README 一行合入；七张 Issue 关闭评论各带 trace-kit 指针；`看板` 窗口跑新脚本；`ux-board.py` / `rc25-board.py` 退役留档 | PR＋Issue | 2 | — |
| T-8 | 发布：`v0.2.0` tag 指向合并 SHA；CHANGELOG 版本段每条带出处与验证口径；plugin 版本 0.2.0；收口评论含证据等级与未验证清单 | tag＋评论 | 4 | — |

## 批终链

本机 `smoke.sh --strict`＋`plugin validate --strict` 绿 → 推送 → kit-selfcheck 绿 → Fable 审①（P1 为零或入修复包）→ codex 两轮结论并入账本 → 修复包 → 复核②（P1 为零、P2 有去向）→ 推送 → 绿 → K-9 四条件落 #17（CI 绿 / ready / 就绪自检 / main 未动且无他 PR）→ 合 main → tag。

## 就绪自检模板（S-8 前）

1. tag `v0.2.0` 指向合并 SHA；kit-selfcheck 绿链接
2. tz 插件版本读回（`claude plugin list`）
3. `看板-v1` 窗口与旧 `看板` 并排 ≥ 1 小时的两份 `--dump` 对照，差异逐条解释（谁对）
4. lingxi 侧：`ux-b1` 当前阶段与开放 PR 数（R-3 判定）
5. 证据源配置试跑命令清单（全部只读）与输出摘要
