# 决策清单（看板 v1 Trace [#17](https://github.com/Moshuiwang/trace-kit/issues/17)）

> 块 A 已于 2026-09-05 13:4x–13:5x 北京由产品负责人在 lingxi 规划者会话给齐（原话逐字）。D-1…D-8 为带默认值的开放项：不回即按默认；改判以 #17 评论单字为准，编排者回写本文件。

## 一、已裁定（块 A）

| # | 事项 | 裁定 | 原话 | 执行侧 |
| --- | --- | --- | --- | --- |
| A-1 | 看板引擎的家 | **trace-kit**；lingxi 只放证据源配置 | 「同意进入 trace-kit」 | §2 / S-4 / S-8 |
| A-2 | 边框轮数 | 审过 1 / 2 / 3 / 3 轮以上各用不同样式，未审细线 | 「审过 2 轮、3 轮、3 轮以上，用不同样式的边框表现出来」 | S-3、#12 v3 第 2 条 |
| A-3 | 简易版要求 | 大模块、少细节、每模块显示 loop 轮数与耗时；先 dump 给 PM 看 | 「我对简易版的要求是大模块，不要那么多细节，但是要能够呈现每个模块 loop 了几轮，花费了多少时间。你可以 dump 出来给我看」 | W0-2 / S-3 |
| A-4 | 模型 | 编排 Fable 5.1 max；核心 Fable xhigh；外围 Opus high；审核 Fable xhigh；外审 codex sol max 两轮 | 「编排甚至可以到 fable 5.1 max」「如果需要外部审核，可以是 codex sol max」 | §5 |
| A-5 | 并行 | 与 lingxi ux-b1 并行，新窗口新编排者 | 「这个 trace 我会并行和 ux-b1 用一个新的编排者执行」 | §6 / R-3 |
| A-6 | 范围 | 10 张：tk #12 v3、#13；lingxi #577–#582、#589；#15 / PR #16 作废 | 「其他无异议，你立即规划」 | 输入表 |

## 二、开放项（默认值，不回按默认）

| # | 事项 | 选项 | **默认** | 何时答 |
| --- | --- | --- | --- | --- |
| D-1 ✅ | 四档边框字形 | 未审 `┌─┐` / 1 轮 `╔═╗` / 2 轮 `┏━┓` / 3 轮 `┏╍┓` / 3 轮以上 `┏╍┓`＋`⟲N`＋醒目色；或 W0-2 快照另提 | **如前** | W0-2 快照过目时 |
| D-2 ✅ | 简易版模块三行 | 干什么（章节＋完成计数）/ 轮数四类 / 证据（PR · 评论数 · 最新时间）；标题行右侧「实际或已跑 / 预估」 | **如前** | W0-2 |
| D-3 | lingxi 证据源配置位置 | `docs/traces/board.toml`（分类器判 full 则接受 1 次 Epic Full）/ 仓外 | **仓内 `docs/traces/board.toml`** | S-8 前 |
| D-4 | tz 插件安装方式 | 本地 clone 路径（钉 tag）/ GitHub 市场 | **本地 clone 路径** | S-8 前 |
| D-5 | v0.2.0 tag | 收口发布 / 缓 | **发布** | S-7 |
| D-6 | #13 三项 | 只改状态位句加 / CODEOWNERS `@Moshuiwang` / 规则集由 PM 操作 | **全默认**（PM 09-02「都按你的来」） | S-1 |
| D-7 ✅ | 简易版是否默认视图 | 默认简易、`v` 切复杂 / 反之 | **默认简易** | W0-2 |
| D-8 | lingxi 收口 PR 次序 | 排在 ux-b1 合 main 之后 / 元守护裁定其他窗口 | **之后** | S-8 |

## 三、现场裁定（编排者随执行登记，附原话与评论指针）

| # | 事项 | 裁定 | 依据 / 指针 |
| --- | --- | --- | --- |
| C-1 | 批分支基线 | `batch/17-board` 从 `9d91a38` 切出（合同写 `300ded8`，多的一个提交即三件套自身） | [接管评论](https://github.com/Moshuiwang/trace-kit/issues/17#issuecomment-5549924168) |
| C-2 | 子代理派发机制 | Agent 工具无法按合同档位设 effort（会话启动后定义不生效）→ 改为独立 `claude -p --model … --effort … --permission-mode auto` 进程，各在自己的 worktree 内；判活只信 PID 与提交 | 本会话 14:3x 实测；档位与合同 §5 一致 |
| C-3 | 并行施工共同基线 | 编排者先写接口约定（本目录 `接口约定.md`）与 `model.py` / CLI 骨架，五个 Step 只改各自归属文件；`model.py` 与约定不由子代理改 | `df21101` / `ac1c620` |
| C-4 | 空项目 kickoff 真实旅程（#13 关卡 2） | 由编排者在 S-7a 集成时做，不交子代理（避免子代理再起模型会话） | S-1 派发卡 |
| C-5 | lingxi 只读证据试跑（R-6） | 只由编排者执行；S-4 子代理只写配置不跑 ssh | 合同 R-6 |
| C-6 | #13 第 1 项与关卡 5 冲突 | 触发条件五条搬进 `kickoff` skill 新小节，合同模板只留一行指针（6137 B < 6336 B）；子代理顺带的三处去重保留 | S-1 报告第 4 节；`e2bf520` |
| C-7 | 轮数归属回落 | 评论首行无 Step ID 时按评论时刻落在哪个模块活动窗口归属（角标推）；都不命中记 Trace 级 | W0-2 真实发现（#606 评论首行无 Step ID）；接口约定 §7.2 |
| C-8 | `dump` 纯文本可读性 | `dump()` 标题行追加中文状态词；TUI 帧不加（#12 v2 裁定 5 只约束 TUI） | 接口约定 §8 |
| C-9 | R-6 只读试跑被拦 | 三条 ssh 命令在本会话被 auto 分类器拦截 → 记「未知」不绕；**元守护澄清（16:04）：不需要 PM 单字，默认处置＝元守护代跑**，16:06 已代跑通过并贴回；运行期看板进程自行执行（可保留 `-n`） | 元守护评论 16:04 / 16:06 |
| C-10 | W0-2 简易版形态（G-1） | **产品负责人 2026-09-05 19:06 北京回「批准」**：冻结三行模块、四档边框字形（D-1）、头六项＋五级阶段行、来源角标一字暗色、默认简易版 `v` 切复杂版（D-2 / D-7 生效） | 原话「批准。」（hb-b1 窗口）；快照评论 5550183045 |
| C-11 | 复核②新增项处置 | N-1（P2：已发布为「否」时预发 / 生产误显「未知」并进阻塞）＝登记 v0.2.1 候选，不再开修复轮（合同 §3 新发现默认只登记；#606 已发布故试穿不受影响）；N-2 / N-3 / N-5 / N-6 可选项登记同处；N-4（CHANGELOG 旧句 91%）随回写改正 | 复核②报告；本批收口评论「已知项」 |
| C-12 | 试穿缺陷 H-1 处置 | 同分支多 PR 身份冲突判「未知」在真实数据上错（#606 #608 已合入＋#610 收口 PR）→ 按合同「试穿缺陷 P1 走热修小 PR」：`hotfix/17-v0.2.1`（H-1 四条规则＋N-1 / N-3）→ PR #20 → v0.2.1；两次 `gh pr merge` 均由产品负责人在 hb-b1 窗口以 `!` 执行（本会话分类器拦） | #17 评论 5551698937 / 5551747396；`1ecb16b` |
