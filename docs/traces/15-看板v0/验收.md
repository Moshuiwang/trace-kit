# 验收（可观察完成标准 + 证据等级目标 / 现值；证据落 Issue #15 或 PR）

> 证据等级口径：产品负责人全局 1–7 级（1 已分析 / 2 已改代码 / 3 本地测试通过 / 4 CI 通过 / 5 已部署 / 6 真实旅程验证 / 7 回源确认）。本 Trace 无部署面；「真实旅程」= 产品负责人在 tmux 里看本 Trace 的看板。每项完成把「现值」改成实际等级并附指针；未验证层级如实列在文末，不得为了收口好看而标绿。

## 逐项关卡

| 项 | 可观察完成标准 | 目标等级 | 现值 | 证据 |
| --- | --- | --- | --- | --- |
| E-1 小修包 | #13 关卡 1–5：`smoke.sh --strict` 绿；`plugin validate --strict` 双绿；空项目 kickoff 真实旅程生成的合同无 §八空表、成本段上限为数字、含「未知 + Owner + 时点」写法；派发卡刹车句、kickoff 自检句、任务表「只改状态位」句到位；本仓 `.github/CODEOWNERS` 存在；`plugin/templates/合同.md` 字节数小于 6336B | 4 | — | — |
| E-1 规则集 | `gh api repos/Moshuiwang/trace-kit/rulesets` 非空（需 PM 在 Settings 操作） | 需 PM | — | — |
| E-2 回放 | `board.py --repo-root . --now 2026-09-02T12:00Z --dump` 对本仓自己发现：02:48→07:47 空档 299 分钟含 219 分钟暂停；`dd53ecf`→PR #9 等待 24m36s；9/9 PR 自合、合同 PR 根节点黄 | 3 | — | — |
| E-2 夹具 | 四个夹具（简单 T0/T1/T2；复杂 F1–F4）在 `kit-selfcheck` 绿 | 4 | — | — |
| E-2 兼容与告警 | Trace #1 与模板示例行不带标签块全部解析；无法解析的行进灰色卡片并计入告警；字数超限只警告、卡片无省略号 | 3 | — | — |
| E-2 刷新与重画 | 一次刷新 ≤10 秒；只重写差异格；150×52 与 75×52 可读 | 3 | — | — |
| E-2 skill 真实旅程 | 空项目冒烟里 `/trace-kit:board --dump` 输出快照 | 6（本机） | — | — |
| 发布 | `v0.2.0` tag 指向 main；CHANGELOG 每资产出处与验证口径；`docs/traces/15-看板v0/看板.txt` 归档 | 4 | — | — |
| 产品负责人验收 | tmux 里看一次本 Trace，30 秒内说出做到哪 / 堵在哪 / 费在哪；其他 pane 光标不跳 | 6（真人） | — | 需 PM 配合 |

## 批终链（全绿才进下一阶段）

本机 `smoke.sh --strict` 绿 → 唯一审核者批量审查（一次，绑定候选 SHA）→ 统一修复包 → 同一审核者定向复核（一次）→ 冻结候选 → `kit-selfcheck` → 合 main → `v0.2.0` tag。冻结后回填：候选 SHA `________`；tag → `________`；kit-selfcheck run `________`。

## 未验证层级（收口时如实列）

- 待填（至少会有：`template/.github/workflows/*` 真实 Actions 运行仍留给第一个采用项目；规则集若产品负责人未操作则「合并即批准」仍是约定）。
