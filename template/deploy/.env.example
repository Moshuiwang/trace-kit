# 部署环境变量样例。**本文件只说明需要哪些变量，不含任何真实值。**
# 出处：lingxi https://github.com/Moshuiwang/lingxi/issues/62；验证：stage 20+ 次升级、prod 首发（2026-09-02）
#
# 一个环境要**三个**文件，不是一个（生产同型：.env.prod / .env.prod.app / .env.prod.migrate）：
#
#   deploy/.env.stage          compose 插值用（镜像仓库与 tag），由 --env-file 读取，**不含凭据**
#   deploy/.env.stage.app      app 进程的凭据与配置
#   deploy/.env.stage.migrate  migrate 作业的凭据（只有迁移连接串）
#
# 三个名字都匹配 .gitignore 的 `.env.*` 规则，因此天然不入库；只放行 `.env.example` 这一个
# 名字（scripts/ci/check_deploy_contract.py 会核对它们没有被提交）。
#
# **为什么按服务拆而不是共用一份**：每个进程只拿自己真正需要的变量。共用一份 env 等于
# 把业务库口令、迁移 DDL 角色、外部应用密钥一起塞给每个进程；进程再把环境继承给子进程
# （执行器、插件、命令行工具）时，凭据就一路流进了它们不该到达的地方。
#
# 镜像里**不预置任何凭据**：Dockerfile 没有一条 ENV 写凭据，全部由运行期注入。
# 凭据不进版本库、不进镜像、不进日志、不进 PR 正文。

# ===========================================================================
# 文件一：deploy/.env.stage —— 只给 compose 做插值，**不含凭据**
# ===========================================================================
# 这个文件由 `--env-file deploy/.env.stage` 读取。`env_file:` 只把变量注入容器，
# **不参与 compose 文件自身的 ${VAR:?} 插值**，所以镜像仓库与 tag 必须走这里。
#
# 镜像仓库前缀（这里换成你的镜像仓库）。
APP_IMAGE_REGISTRY=ghcr.io/your-org
# 不可变镜像 tag：<YYYYMMDD>-<commit sha 前 12 位>，语义见 deploy/README.md。
# 回滚就是把这一行改回上一个 tag 再 `up -d`。禁止 latest 或分支名。
APP_IMAGE_TAG=20260101-0123456789ab

# 可选：镜像 digest 固定（操作细节以 deploy/生产部署runbook.md「镜像 digest 固定」为准，
# 这里只登记变量名）。镜像引用在 APP_IMAGE_TAG 之后追加了这个可选后缀，默认空——不设时
# 渲染结果与没有这一段逐字节一致。值的形状是 `@sha256:<digest>`（含前导 `@`）：
# tag@digest 是合法的镜像引用，digest 优先生效、tag 仍供人读。
# **不要把 digest 写进上面的 APP_IMAGE_TAG 本身**：多镜像时各有独立 digest，共用同一个
# TAG 变量只能固定其中一个，其余服务会解析出错误的 digest（manifest unknown）。
# APP_IMAGE_DIGEST=@sha256:<本批次镜像 digest>

# 可选：资源限制。compose 文件已给出各服务、各环境的默认值（不设这些变量也能 `up`），
# 完整说明见 deploy/README.md「资源限制」；只在需要单独调整某个服务时覆盖。
# **下面是 stage 建议值，行尾注了生产建议值**——直接把这一整段原样复制进 deploy/.env.prod
# 会把生产静默降到 stage 量级（`:-` 有默认值形态漏配不报错，只会安静退回默认）。
# APP_CPU_LIMIT=0.5              # stage；生产改 1.0
# APP_MEM_LIMIT=512M             # stage；生产改 1G
# APP_PIDS_LIMIT=256             # stage；生产改 512
# APP_MIGRATE_CPU_LIMIT=0.5      # stage；生产改 1.0
# APP_MIGRATE_MEM_LIMIT=512M     # stage；生产改 1G
# APP_MIGRATE_PIDS_LIMIT=256     # stage；生产改 512

# ===========================================================================
# 文件二：deploy/.env.stage.app —— app 进程的凭据与配置
# ===========================================================================
# 这里换成你的 app 进程真正需要的变量；只装它需要的，一个都不多给。
#
# 业务数据库连接串（运行角色：无 DDL 权限）。示例值里保留 connect_timeout 一类超时参数
# 作为与连接层默认值的对账基线；停机预算（compose 的 stop_grace_period）必须覆盖这些
# 超时的合法最坏值，见 deploy/README.md「停止宽限期」。
# APP_DATABASE_DSN=<业务库连接串，勿提交>
#
# 注意：APP_DATA_DIR **不在这里设**。它由 deploy/compose.yaml 固定为持久卷内路径，
# 改成容器内路径会让每次镜像替换都丢失数据。

# ===========================================================================
# 文件三：deploy/.env.stage.migrate —— 数据库迁移作业
# ===========================================================================
# 与业务的连接串是**两个变量**，迁移不回落到后者：迁移要 DDL 权限、业务进程不要，
# 两者本就该是不同的数据库角色。迁移是一次性作业、不受 stop_grace_period 约束，
# 因此这里不要求 statement_timeout——给迁移设语句超时反而会让一条正常的大表 DDL 中途失败。
# APP_MIGRATION_DSN=<迁移库连接串（DDL 角色），勿提交>
