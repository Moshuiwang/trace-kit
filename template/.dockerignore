# 构建上下文排除清单。
# 出处：lingxi https://github.com/Moshuiwang/lingxi/issues/62（可复现构建、路径不泄漏、凭据不进镜像）；验证：每次 Epic Full image 作业与生产首发。
#
# 这份清单同时服务三条断言，不只是"让上下文小一点"：
#   - 可复现构建：`__pycache__` 里的 .pyc 是在宿主机上编译的，内嵌宿主机绝对路径；
#     两个不同路径的干净 clone 各构建一次，若把它们 COPY 进去，两份 rootfs 必然不同。
#   - 路径不泄漏：同上，构建目录的绝对路径会随 .pyc 进入镜像。
#   - 凭据不进镜像：`.env*` 是本地与受控测试的真实凭据载体（见 .gitignore）。
#
# 排除是**默认拒绝**：先排除一切与构建无关的目录，再按 Dockerfile 的 COPY 精确取用。
# 这里加你的：新增顶层目录若不进镜像，在下面登记一行。

# 版本控制与代理工作现场
.git
.gitignore
.claude
.github

# 凭据与本地配置（.env.example 也不需要进镜像）
.env
.env.*
*.env

# Python 本地产物。**必须带 `**/` 前缀**：Docker 的排除模式按整条相对路径匹配，
# `*` 不跨 `/`，裸写 `__pycache__` 只能匹配上下文根目录下的那一个。
**/__pycache__
**/*.py[cod]
**/*.egg-info
build
dist
**/.venv
.dev-check
.tmp

# 非生产资产：门禁脚本、测试、文档、部署编排（compose 在宿主机上执行）
scripts
tests
docs
deploy
Dockerfile
.dockerignore
*.md
