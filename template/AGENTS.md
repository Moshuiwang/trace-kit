# <项目名>：代理工作约定

> 出处：lingxi [AGENTS.md](https://github.com/Moshuiwang/lingxi/blob/caa845d77fbd2d6381de304dc6047498aa84c782/AGENTS.md)（f99bdf5 起 24 次修订）；验证：全仓每次任务读取；分档 G2（结构与底线原样，产品名词换占位）。

本文件只放每次任务都需要的入口与底线。产品事实、协作流程和验证细则按任务读取；不要为了普通对话或局部改动把整套文档读入上下文。

## 按需读取

| 任务情况 | 开始前必须读取 |
| --- | --- |
| 要改变或说明用户体验、业务规则、权限、安全、身份或外部平台边界 | [产品合同](docs/产品合同.md) |
| 要声称、验收或改变“现在已经具备”的能力与限制 | [当前能力](docs/当前能力.md) |
| 要形成、替代或撤销长期产品选择 | 相关[长期决策记录](docs/决策记录/README.md) |
| 要新建或重组产品文档、不确定结论应留在仓库还是 Issue，或准备关闭 Issue | [产品文档索引](docs/README.md) |
| 要实现或修改正式代码、编写测试或进行技术审查 | [验证与门禁](docs/技术设计/验证与门禁.md)（这里换成你的：如有代码框架文档，一并列入）；只在认领产品或部署断言时按需读取[验收矩阵](docs/技术设计/验收矩阵.md) |
| 要创建或编辑 Issue、PR、Project、评论，准备 Draft PR，或进行多人、跨设备、跨 PR、高风险协作 | [协作约定](docs/协作约定.md)；技术审查同时读取[验证与门禁](docs/技术设计/验证与门禁.md) |
| 要规划或修订持续执行的多 Issue、多 Epic、多角色、顺序依赖或高风险 Plan | [长期执行方法入口](docs/协作/执行方法.md)所指的方法正文，由规划者据此生成或修订具体 `[tracking]` Execution Trace |
| 要编排已经批准的长期 Plan | 已批准的 `[tracking]` Execution Trace（[docs/traces/](docs/traces/README.md) 三件套）及其中绑定的来源；方法正文只供规划者生成或修订具体 Trace，不作为编排者补合同的后备正文；Trace 缺项时退回规划者修订 |
| 任务关联已有 Issue、PR 或 Project | 仅相关的 GitHub 工作项 |

普通问答、头脑风暴、与产品边界无关的局部修复，或纯机械改动，**不读取上述产品文档和 GitHub 工作项**。用户指定的文件、目录规则和离改动最近的 `AGENTS.md` 优先；只读取完成当前任务所需的最小集合。

## 工作底线

- 默认使用中文；面向产品负责人时优先说明用户可见结果、成功 / 等待 / 失败和待选择事项。
- 仓库 `docs/` 是长期产品与技术事实的唯一正文；Issue / PR / Project 分别记录可关闭的工作、实现审查和进度。唯一例外是长期执行方法：其完整正文维护在套件 `METHOD.md` 的固定版本（入口见 [docs/协作/执行方法.md](docs/协作/执行方法.md)），仓库中只保留稳定跳转，不复制正文。
- 不把旧代码、参考项目或模型推测当作产品决定；外部平台的身份、权限、能力和失败语义以有效规范为准。
- 遇到会改变用户承诺、扩大权限或数据范围、产生不可逆外部副作用，或外部规范不明确的选择时，停止该选择并请产品负责人决定；可继续不依赖该选择的低风险工作。
- 小型、可逆且不改变用户体验、权限、数据或外部依赖边界的改动可直接实施；其他变更按[协作约定](docs/协作约定.md)建立工作项并完成开工前事实确认。
- 改动后运行与风险相称的验证，并简要交代用户得到什么、如何验证、剩余风险或待决策。
- **临时进程、容器、worktree、测试实例谁建谁清**：为诊断、验证、探测而启动的任何常驻物，必须在当次任务收口前停掉并确认已停；确实要留，就在收口说明里写清“留了什么、为什么留、谁负责清、怎么清”，并登记到对应工作项。**残留的临时实例不是无害的**——一次真实验收中，一个为诊断临时起的进程与正式入口抢占同一条独占外部通道（新连接踢掉旧连接），**静默劫持并烧掉了产品负责人的一次性授权**，用户侧表现为“点了授权、页面显示处理中、后台什么也没发生”；排查时它还制造了误导性现象（另一台机器上的僵尸进程持旧凭据反复被拒，被误读为“服务在疯狂重连”）。因此：**共享的外部通道（长连接 / 队列 / 回调通道 / 独占令牌）同一时刻只允许一个客户端**；起临时实例前先查有没有同类实例在跑，用完即停。共享 Git 工作树同理：**做变异实测或跑测试的并行代理必须使用独立 worktree**，同一工作树同一时刻只允许一个写入者（实测事故：编排者并发提交把审查代理的变异行卷进提交，险些推送坏提交）。长 Plan 的收尾必须包含一项“环境清理与残留盘点”，逐台确认无遗留临时进程 / 容器 / 实例。出处：lingxi 两次事故的规则落地提交 [2026-08-09 通道劫持](https://github.com/Moshuiwang/lingxi/commit/cf109faa7a5e313de6ba0e97f0fb427d83c4add4) 与 [2026-08-24 PR #299 并发提交](https://github.com/Moshuiwang/lingxi/pull/299)。

## Code Review Rules

- 不得在生产环境现场构建、硬编码生产路径或配置、人工修改生产文件。安全路径是使用已经验收并冻结的制品，通过外部配置发布。
- 涉及服务、数据或外部交付时，重启与重试不得造成用户结果丢失、重复交付或无法恢复；改动必须提供可验证的恢复路径。
