<!-- trace-kit：执行方法正文。本文件为源 Issue 的原样搬运，正文一字未改；修订只在源 Issue 进行，再按版本同步到这里。 -->

> **trace-kit 版本头**：方法版本 **v19**（2026-09-09）。源 = GitHub lingxi Issue [#678「长期执行计划与 Execution Trace」](https://github.com/Moshuiwang/lingxi/issues/678)（现行唯一正文与修订入口；旧源 [#147](https://github.com/Moshuiwang/lingxi/issues/147) 已降为历史正文，只保留 v17 及更早版本）。搬运时间 2026-09-10（源 Issue 最后更新 2026-09-10T08:36:49Z）。正文中的「本 Issue」「评论」指源 Issue；「仓库 `docs/traces/`」指采用本方法的项目仓库；「验证与门禁」「协作约定」指该项目的对应文档（骨架见本套件 `template/docs/`）。

# 长期执行计划与 Execution Trace 模板 v19

> **状态：现行唯一方法入口，v19（2026-09-09）。**沿用 v9→v19 编号；来源是 [#147 的 v17 历史正文](https://github.com/Moshuiwang/lingxi/issues/147)、[#665 已批准的 v18 第 1–8 项](https://github.com/Moshuiwang/lingxi/issues/665#issuecomment-5567200170)（欠账已于 r3 结清），及 v19 的运行面通用化与三条新条款。**本版起模板不绑定单一执行者**：方法正文对任何执行者成立，运行面差异见第四节各自小节。
>
> **阅读对象：规划者。**本模板不能直接授权执行。规划者据此生成具体 `[tracking]` Execution Trace；产品负责人批准明确版本后，交给独立的顶层编排任务。编排者只按获批 Trace 和其中链接的来源推进，不能从本模板、聊天或历史评论补范围与授权。
>
> **运行面由具体 Trace 绑定**，在开工快照里写明执行者、执行机、操作系统与时钟基准。远端研发、受控验收、外审和发布按具体 Trace 接入。已经批准的历史 Trace 继续使用其绑定版本，不自动迁移。

## TO PM

具体 Trace 只写这次工作会变化的结果、范围、步骤、决定、证据和恢复点。项目固定规则、执行者的工具教程以及 GitHub 已有记录不再重复抄写。产品负责人一次批准可覆盖到明确终点；新增权限、数据范围、不可逆动作或未获批外部写入仍须另行决定。

## 一、什么时候需要 Trace

满足多 Epic、多批次、顺序依赖、跨任务接力，或涉及部署、权限、数据、真实外部系统时使用 Trace。单一低风险 Story / Task 直接使用原 Issue。

具体 Trace 必须选择一个授权终点，例如：仅提交、分支已推送、PR 可合并、已合并、不可变候选已形成、stage 已部署、stage 已验收或生产已发布。达到前一项不能自动声称后一项成立；模板本身不授予 stage 或生产写入权限。

### 角色

| 角色 | 职责 |
| --- | --- |
| 产品负责人 | 决定结果、范围、优先级、权限、数据和授权终点；批准 Trace 与必要变更 |
| 规划者 | 核事实与可达性，形成最小合同；批准后停止实施，可在登记权限退出后转为只读监护者 |
| 编排者 | 只派 Ready Step，维护恢复点，冻结候选，组织审核、验收和收口；不补合同 |
| 实施者 | 在分配范围内实现、自测和修复；不扩大范围 |
| 验收者 | 对约定对象执行验收并解释证据；不修改产品代码 |
| 审核者 | 对固定候选独立判断；不参与实现，不直接修复 |

Step 的 Owner、候选审核记录、PR / commit / run 链接和交接快照构成主链记录，不建立逐动作角色流水账。

## 二、正文分层与载体

| 层 | 写什么 |
| --- | --- |
| 固定规则 | 本 Issue、仓库 `AGENTS.md` 和验证与门禁；具体 Trace 不复制 |
| Trace 合同 | 本次结果、范围、授权终点、步骤、Gate，以及命中的例外 |
| 运行事实 | 当前状态、候选、证据、等待与清理；只在状态变化、决策、批次收口或交接时更新 |

每个 Trace 使用 `docs/traces/<Issue号>-<短名>/`：

- `合同.md` 必需：最小合同与步骤；建立或改变范围、授权、验收标准时走 PR，按仓库批准规则生效。
- `任务表.md` 必需：Step 状态和三行恢复点；不复制合同全文。
- `验收.md` 按需：只有存在 Trace 级新增断言、跨 Epic 总验收或需要集中证据索引时创建；否则直接链接成员 Issue / PR 的验收。
- `[tracking]` Issue 只写目标、文件链接和阶段；评论承载产品裁定、批次收口与交接。

新合同以 **24KB** 为目标；超过 **32KB** 时先去重、改为权威链接或拆来源索引，再说明必须保留的内容；**40KB 是硬上限**，超出即拆——把「现场合同来源」「开工能力与环境快照」「批准前可达性审计」三张表移入同目录的 `开工快照.md`，合同正文只留六段、计划执行步骤、覆盖对照表与裁定清单。不得把授权与验收门槛移出合同来绕过批准。已有 Trace 不强制改写。

## 三、规划者只回答必要问题

1. **结果：**产品负责人最终得到什么，唯一授权终点是什么，怎样证明达到。
2. **范围：**输入 Issue、明确不做、会影响用户承诺的边界及关闭去向。
3. **步骤：**每个 Step 的可观察产物、Ready 条件、Owner / 工作区、通过证据、成功后继和失败 / 等待去向。
4. **触发项：**外部写、共享资源、跨任务、独立审核、特殊预算分别写有 / 无；只有“有”才展开。
5. **批准：**至少一个 Step 可立即 Ready；未知项有 Owner 与补齐条件，受它影响的 Step 保持 Pre-ready。

执行图只在它比步骤表更清楚时使用。依赖必须能说明传递的制品，或占用的独占资源 / 额度；否则删除。多个 Epic 共同形成一个用户结果时，保留最终集成、验收或发布 Step。

### 人类 Gate 与预算

目标、范围、产品承诺、权限、数据、不可逆动作和外部写入必须在受影响 Step 前获批。已知且可逆的动作可以预授权，但须写触发条件、批准动作、有效期和不回复时的停止位置；沉默不能产生新授权。不阻塞里程碑的建议留到收口集中处理。

默认每个固定候选只做一次适用的完整门禁和一次约定的独立审核；真实验收只在约定窗口执行。Trace 写预计批次、候选 / 重检查次数、外审或 stage 窗口，以及明确的时间或费用上限。**规划期给量级预估（子代理人次、token 量级、PR 数、CI 次数），收口评论逐项对账**——只给上限不给预估，事后无法判断上限定得准不准。**PR 次数按批次数 + 2 估**，超出需说明；每个 PR 至少消耗一次完整检查。到线时完成当前安全单元、保存证据、释放资源，再请求预算或范围调整，不能靠跳过失败或降低证据等级省预算。

### 命中才展开的合同

- **共享资源：**只记录实际会冲突的数据库、机器人、端口、队列、令牌、stage、合并道或工作树；写唯一 Owner、取得条件、释放与清理。同一工作树同一时刻只有一个写入者；并行变异或重检查必须隔离目录和依赖。
- **外部写与发布：**写目标、获批动作、固定制品、恢复方法和回读证据。审核对象、验收对象与发布对象必须对应；候选 SHA / digest 变化后，旧结论按影响失效。
- **真实验收：**只写本次断言、证据层级和实际观测入口。页面可见性与平台客观状态分别取证；测试替身、内部成功状态或绿色 CI 不能冒充真实用户结果。
- **代价陈述的事实基础：**合同与工作项里「不应用的代价」「现在做不到什么」这类陈述必须回源到直接证据，不得由相邻组件的状态推导。相邻不等于同一条链路——某个组件未启用，推不出它所在的那类能力不存在。写不出直接证据时如实写「未知」与取证方法。
- **实测用例的副作用：**任何将在真实环境执行的用例，写完后必须逐步回源核对其执行分支是否含不可逆操作（硬删除、外部发送、权限变更、不可回滚迁移）。命中即改用可逆路径，或停止并上报。**「有还原步骤」不等于可逆**——还原只覆盖它自己那一步，不覆盖同一分支里的其他副作用。**转抄核对必须核语义，不能只核字面**（格式、列数、指纹一致不构成安全性证据）。
- **首触冒烟：**正式验收前，在安全且可恢复时先走一次非正式真实路径；没有可用注入口时并入第一条正式用例，失败进入缺陷处理，不把模拟重放当成已部署冒烟。
- **主备路线：**只有主路线失效会阻塞且备用路线真实可用时才写；切换会改变权限、独立性或证据时停止并请产品负责人决定。

## 四、编排方式

默认由一个顶层编排任务持续推进，独立且有边界的实施、调查或验收可交子代理。启用任何常驻机制（后台任务、定时巡检、跨会话接力）须有产品负责人的明确请求或已批准 Trace 授权。

下面第一段是与执行者无关的通用纪律，其后两小节分别是两个运行面的原语；**Trace 只写自己绑定的那一节**。

### 通用编排纪律

**兜底观察的有效性判据**：给外部等待挂观察时，循环条件必须能区分「目标达成」与「查询本身失败」。只判断「输出里没有进行中标记」不成立——查询失败的错误输出里同样没有那个标记，会被当成完成。观察脚本必须显式校验响应可解析，不可解析时继续重试而不是判定完成，并覆盖成功、失败与超时三种结局。**「挂了观察哨」不等于「观察哨会叫醒我」**，判活只信外部证据这条同样适用于判自己的兜底是否还活着。

### Codex Desktop 运行面

| 方式 | 含义与边界 |
| --- | --- |
| 子代理 | 当前任务内的独立工作；可共享文件系统，不自动获得独立工作树；实施、验收和审核子代理不得继续下派 |
| 任务间消息 / 等待 | 向已确认的任务传递提示并观察终态；消息送达不等于对方开始或结果通过，也不产生新授权 |
| 新建顶层任务 | 用于规划→编排、独立审核或确需换任；新任务只取得明确交接材料和获批权限 |
| Fork | 复制已完成历史用于探索；不能冒充空白独立审核，也不完成租约转移 |
| Handoff | 移动同一任务及 Git 状态的位置；不是换角色，完成后仍须核环境和资源 |

heartbeat 只负责按约定观察外部等待；它不授予后续动作权限，电脑、客户端或网络不可用时不得承诺持续运行。具体工具参数以当时的 Codex 工具说明为准，不写进 Trace。

### Claude Code 运行面

编排原语与角色流程由 `trace-kit` 插件承载（开工合同、派发卡、元守护、交接、接管、看板），**模板不重复其正文**；Trace 只写本次绑定的角色与授权终点。与上一节的关键差异：子代理由主代理派发且不得再下派，完成时主代理收到通知；跨会话接力走 tmux 窗口，同一时刻只允许一个写入者；**后台任务与观察脚本的运行环境可能不具备网络访问**，需要查询外部状态的观察哨必须选用具备网络的机制并按上面的有效性判据自检。具体工具参数以当时的运行时说明为准，不写进 Trace。

### 派发、审核与验证

派发只需要六项：Step、可观察产物、范围、Owner / 工作区、通过证据、失败去向。**单个子代理任务预计超过 60 分钟即必须切分**；派发前给出预计时长，超时按兜底观察处置——长任务会阻塞编排者会话本身，期间编排者收不到任何新消息。授权内工作一路做完并等检查终态；需要产品取舍、扩大范围或新增外部副作用时停止。新发现只有阻塞当前里程碑才进入当前批，其余登记到既有载体。

每个固定候选由一名未参与实现的审核者审查；实施者修复，同一审核者定向复核。

**假证据六型（每个固定候选必查）**：①测试替身硬写被测判据，绕开真实规则表；②守卫按路径扫源码，实现搬走后扫的不是持有实现的文件；③变异锚点指向已删符号；④契约测试被条件跳过；⑤测试名、docstring 或验收断言里的无证据陈述（含「已实测」「已复核」类字样）；⑥断言与实现相反。**判据**：删掉被验证的那一行，测试仍全绿 ⇒ 该用例证明的是它自己注入的假设。**断言总数不减少不构成覆盖面证据。**候选变化后按影响重验。红色结果须保留并诊断，不能反复重跑到绿后丢弃。两条并行分支改到共同文件时，在独立工作树先做合并演练。

本机检查只证明实际跑过的范围；与执行机操作系统或架构相关的进程、权限、cgroup、目标架构镜像与生产恢复转到 CI、研发环境或受控环境。`scripts/dev/check.sh full` 只对应完整 CI 的主 gate，不代表 extras、镜像或 stage 已通过。重检查默认串行；只有目录、数据库、端口和安装目录隔离并实测有收益时才并行。

独立外审可在实时小样确认后选择远端 Claude 或本机 agy；一名审核者即可，不默认两边各跑一轮。正式审核必须能读取完整固定候选，记录发现、证据、影响和未验证范围。通道小样只证明可调用，不证明仓库候选已通过。

## 五、状态、恢复与收口

Step 使用 `Pre-ready / Ready / Running / Waiting / Done`。编排者只派 Ready；合同缺项或范围、授权、验收标准变化时，停止受影响 Step 并退回规划者修订，不受影响的 Ready Step 可继续。

CI、远端命令、部署和队列必须回读实际对象的终态；处于等待时记录对象、观察入口和失败去向，不能因“已发起”判定完成。时间记录使用系统实取的日期、时区和起止点，不把并行耗时相加。

`任务表.md` 顶部始终保留三行：

- **当前：**阶段、Step、固定候选和最后一条通过证据。
- **下一步：**唯一下一 Ready Step、Owner 和入口。
- **等待 / 占用：**阻塞条件、决定人，以及仍持有资源和清理方法；没有则写“无”。

跨任务接力时，继任者先读合同与恢复点并确认接管；旧编排者确认停止写入、释放租约后退场，不能出现两个写入者。任务消息没有原子锁，未确认转移前保持等待。

批次收口只记录：达到的结果、候选与检查、剩余 / 下一步、待决定项、资源清理。主链从 Step Owner、PR / commit / CI、审核记录和交接快照复盘，不重复逐命令日志。

最终 `Complete` 只表示全部获批范围达到唯一授权终点。`Partially Delivered / Stopped / Superseded` 须由产品负责人决定，并给每项未完成工作一个明确去向。谁创建临时进程、容器、工作树、测试实例或自动化，谁在收口前清理并回读；需要保留时写 Owner、原因和清理方法。

## 六、Ready 自检

- [ ] 结果、范围、不做清单、唯一授权终点和批准版本明确。
- [ ] 至少一个 Step Ready；其他 Step 的进入、成功和失败 / 等待去向明确。
- [ ] 人类 Gate 已前置或预授权；关键未知没有被写成 Ready。
- [ ] 命中的共享资源、外部写、候选、验收、审核与恢复可执行。
- [ ] **新写的启动条件、完成条件与清零判据已对照它所依赖的对象集合自检**：不自指、不与被它依赖的对象互相等待。判据里点名的集合若包含本单自身或包含「要等本单完成才能收口」的对象，即为死结，须显式写排除项。
- [ ] **开工快照写明执行机的时钟基准与时区**；面向产品负责人的时刻一律双标注且两个值都实读，跨夜任务核日期不只核时分。
- [ ] 编排者从合同与任务表能回答：现在做什么、谁做、何时可做、怎样算过、失败去哪、何时必须停。

## 七、可复制的最小结构

`[tracking]` Issue 只放目标、文件链接和阶段。以下分别写入 `合同.md` 与 `任务表.md`；未命中的附加项不建空表。

```markdown
# Trace：一句话结果

## 给产品负责人
- 结果与授权终点：
- 范围 / 不做：
- 待决定或已预授权的 Gate：无 / G-…
- 方法：#678 v18 / r2；Trace 版本、批准时间与证据：
- 输入、基线与完成标准链接：
- 触发：外部写=无；共享资源=无；跨任务=无；独立审核=无；特殊预算=无

## 执行步骤
| ID | 产物 | Ready 条件 / Gate | Owner / 工作区 | 通过证据 | 后继 / 失败去向 |
| --- | --- | --- | --- | --- | --- |

## 命中才展开
- 外部写与恢复：
- 共享资源与释放：
- 独立审核 / 跨任务接力 / 特殊预算：

# 任务表
- 当前：
- 下一步：
- 等待 / 占用：无
- [ ] S-… 状态｜一句话｜关键证据链接

# 收口
- 达到的终点与证据；未完成去向；清理结果：
```

## 八、版本与来源

- **v19，2026-09-09：**两件事。**其一，模板不再绑定单一执行者**：标题与状态行去掉 Codex 绑定，运行面改为由具体 Trace 在开工快照里声明；第四节拆为「通用编排纪律 ＋ Codex Desktop 运行面 ＋ Claude Code 运行面」，Trace 只写自己绑定的那一节；「Mac 本机检查」等表述中性化。**其二，新增三条**：兜底观察的有效性判据（第四节）、判据自指与循环自检（Ready 门）、代价陈述的事实基础（合同区）。

  **本版的证据边界，如实登记**：三条新条款的实证来自 2026-09-09 一次 **Claude Code 编排会话，而非正式 Trace 复盘**——观察哨因运行环境无网络而失效且以成功状态结束、新写的启动条件与它所依赖的对象互相等待、工作项的代价陈述由相邻组件状态推导而与事实相反。三者都是当场坐实的一手事实，但样本是单次非 Trace 会话，**对长 Trace 场景的适用性尚未经真实 Trace 验证**。

  **未做的两件，一并登记**：①[#665](https://github.com/Moshuiwang/lingxi/issues/665) 第 9 项候选（巡检改事件驱动 K、评论角色前缀 N、元守护职责下沉为仓库脚本 O）**仍无新证据，继续保持候选**，不因换版号而默认进入；②**反向审计（§九日落条款要求的删除侧）本版未做**——它需要「哪些机制在本轮一次都没被引用」的执行数据，而 2.4.0 批次的复盘统计尚未产出。因此本版是净增（相对 r3 增约 820 字符，总量 7.9k 字符，为 v17 上限 14,829 字符的 54%），**净减法的删除侧欠到下一版**，下次修订须先补反向审计再谈新增。

- **r3，2026-09-09：**结清 r2 精简造成的欠账。[#665](https://github.com/Moshuiwang/lingxi/issues/665) 的结果核对实测「候选 1–8 在正文中 4 条完整、5 条打折、2 条丢失」，产品负责人同日裁定四项全部按原批准形态补足：**补回候选 B**（实测用例副作用可逆性回源核）与 **M**（子代理单体超 60 分钟必须切分）——这两条是该轮唯一有事故支撑的增量，r2 删除时正文尚余六成体量，删除并非出于体量压力；**补回候选 A 的假证据六型枚举**（此前实质散落、清单本身不存在，而枚举正是该候选的交付物）；**候选 F 落入 Ready 自检**（此前规则在正文但不在 Ready 门，开工时不会被检查）；**候选 L 补回 40KB 硬上限**，保留 r2 自行收紧的 24KB 目标与 32KB 去重线（软目标无强制力，82KB 那次事故缺的正是硬线）；**候选 J 补回 PR 次数上限与用量预估对账**。
- **v18（Codex）r2，2026-09-09：**按产品负责人批准的[第二轮精简审查](https://github.com/Moshuiwang/lingxi/issues/678#issuecomment-5588694204)重写。删除工具教程、固定项目规则和重复运行字段；正文目标净减 30%；具体 Trace 只写会改变路线的变量，验收文件改为按需。
- **r1.1，2026-09-09：**本 Issue 升为唯一入口，#147 降为历史。
- **r1，2026-09-08：**纳入 #665 已批准 1–8 项并完成 Codex 适配；形成与实测证据见本 Issue 评论。
- **v17 及更早版本：**见 [#147](https://github.com/Moshuiwang/lingxi/issues/147)。

Codex 能力以运行时工具说明和 OpenAI 当前官方文档为准；仓库验证以[验证与门禁](https://github.com/Moshuiwang/lingxi/blob/main/docs/技术设计/验证与门禁.md)及[本机检查说明](https://github.com/Moshuiwang/lingxi/blob/main/scripts/dev/README.md)为准。能力核验与一次性证据留在本 Issue 评论，不进入模板正文。


