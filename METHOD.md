<!-- trace-kit：执行方法正文。本文件为源 Issue 的原样搬运，正文一字未改；修订只在源 Issue 进行，再按版本同步到这里。 -->

> **trace-kit 版本头**：方法版本 **v16**（2026-08-31）。源 = GitHub lingxi Issue [#147「长期执行计划与 Execution Trace」](https://github.com/Moshuiwang/lingxi/issues/147)（唯一正文与修订入口）。搬运时间 2026-09-02（源 Issue 最后更新 2026-08-31T06:09:17Z）。正文中的「本 Issue」「评论」指源 Issue；「仓库 `docs/traces/`」指采用本方法的项目仓库；「验证与门禁」「协作约定」指该项目的对应文档（骨架见本套件 `template/docs/`）。

# 长期执行计划与 Execution Trace 模板 v16

> **状态：现行版本 v16。**本 Issue 是方法唯一正文与修订入口。产品负责人 2026-08-31 授权按 rc22 复盘建议升级：常驻 session＋批次命名 window＋元守护正文化（含失联判定与模型偏离异常信号）；临时授权带失效时点；变异验红硬姿势。正文不超过 v15。
>
> **阅读对象：规划者。**本文不是可直接执行的 Plan，也不是编排者现场补合同的规则库。规划者读取本文、产品与技术事实和实时能力后，生成具体带版本的 `[tracking]` Issue——即 **Execution Trace**。
>
> **交接对象：编排者。**产品负责人批准某一版 Execution Trace 后，编排者只按获批 Trace 及其绑定来源执行；字段缺失时退回规划者修订，不从本模板、聊天或历史评论自行补齐。
>
> **Trace 不是逐命令日志**——它是执行合同、状态账和恢复入口；一次性命令与实现细节留在 Story Issue / PR。
>
> **适用范围：**持续推进多个已决策 Issue，或含多 Epic、多角色、顺序依赖、共享资源、高风险验收的长期工作。单一低风险 Story / Task 直接使用原 Issue。

## TO PM

- **应用后得到什么：**规划者先证明目标可达、外部决定前置、实施与验收分账，再生成带稳定 Step ID 和成功 / 失败 / 等待分支的 Trace；编排者拿到可直接路由、可中断恢复、可如实收口的合同。
- **重点避免什么：**不把缺少大量实现的工作写成短期验收；不把组件存在或 CI 通过冒充用户结果；不让未知、通道冲突或制品口径差异拖到批次尾声才暴露；不让支线吃掉主线时间与验证预算。
- **批准的是什么：**产品负责人批准的是规划者产出的某一版具体 Execution Trace、其中绑定的范围、外部动作和授权终点，不是笼统批准本模板，也不是逐 Epic 重复开工。

## 一、规划者五分钟使用法

1. **回读事实：**核对产品决定、输入 Issue、基线、机器 / 环境、权限、外部规范和适用门禁。
2. **证明可达：**逐项检查输入 Issue 怎样关闭、真实结果链缺什么、哪些是实施、哪些才是验收。
3. **前置未知：**把可能推翻架构、合同、权限或关键估算的无副作用研究放入 Wave 0；依赖项保持 Pre-ready。
4. **形成路线：**拆出 Epic / Story / Task，为每一步分配稳定 Step ID，写启动条件、主 / 备路线、资源租约、成功证据、成功后和失败 / 等待后去向。
5. **实例化合同：**写清冻结对象、验收、审核 / 修复循环、授权、时间预警、关闭资格和尾项转交。
6. **过 Ready 门：**完成第五节自检，写入并读回具体 `[tracking]` Issue，取得产品负责人对明确版本的批准，再交给编排者。

规划者在第 6 步完成后必须停止，不得在同一任务 / 会话顺手开始实施；即使产品负责人同时授权执行，也必须把获批 Trace 交给一个全新、独立的顶层编排任务 / 会话。

## 二、模板、Trace 与执行的交接

两层合同只此一处定义：**本模板只供规划者生成具体 Trace；获批的 `[tracking]` Trace 才是编排者的合同。**一份 Execution Trace 必须同时具备：

- **合同性：**目标、范围、来源、角色、证据、授权和终点明确；
- **可执行性：**至少一个 Step 已 Ready，其他 Step 有明确进入条件；
- **可路由性：**每个 Step 有 Owner / 路线、所需资源、成功证据和后继分支；
- **可恢复性：**换一个编排者，只读 Trace 正文与最近一条批次收口 / 交接评论即可知道当前候选、等待、缺陷和下一步；
- **可收口性：**每个输入 Issue、发现、未完成范围和临时资源都有去向。

Trace 的两个维护区域（v13 起动态事实在评论流维护——v12 正文动态区在 #203 实测零维护，恢复全靠评论）：

| 区域 | 载体与维护者 | 规则 |
| --- | --- | --- |
| 合同区 | 仓库 `docs/traces/<issue号>-<短名>/` 三件套（合同.md / 任务表.md / 验收.md）；规划者在批准前写完（载体规范见 docs/traces/README.md；历史 Trace #46~#304 不迁移） | 合同修订走 PR，**PR 合并即批准**；任务表状态=文件当前值随执行 commit；`[tracking]` Issue 降级为瘦指针（目标一句话+三件套链接+当前阶段），保留裁定 / 交接 / 批次收口评论界面 |
| 动态事实 | 评论流；编排者维护 | 以带 UTC 时间戳的**批次收口评论**、**交接评论**和**决策留痕评论**为唯一动态事实（纪律见第六节 6.7）；不能修改合同区，不能用无来源的动态记录扩大范围 |

两区使用同一个稳定 `Step ID`。实际工作无法映射到获批 Step 时，说明合同需要由规划者修订。

## 三、工作层级、交付等级与角色

### 3.1 四层工作

| 层级 | 回答的问题 | 默认载体与交付 |
| --- | --- | --- |
| Plan / Execution Trace | 这段长期工作清空什么，如何推进，最终交付和授权终点是什么 | 一个持续维护的 `[tracking]` Issue |
| Epic | 哪组 Story 共同形成一个可独立冻结、验收和交付的结果 | `[epic]` Issue、候选分支、最终 PR，或明确的研究 / 决策结果 |
| Story | 用户、管理员或系统可观察到的一段完整价值或工程结果 | `[story]` Issue、一个可验证切片 |
| Task | 完成 Story 所需的具体动作 | 待办、提交或代理子任务；需要独立跟踪时才建 `[task]` Issue |

Epic 必须注明类型：`Research / Decision`、`Implementation / Integration`、`Acceptance`、`Release / Ops`。混合 Epic 各阶段必须分账；仍缺生产代码、调用方、运行时装配或部署形态的 Epic 不能伪装成纯 `Acceptance`。

### 3.2 交付终点

每个 Epic 同时记录当前生命周期、计划交付终点、实际达到的终点：

| 交付终点 | 适用工作 | 成立条件 |
| --- | --- | --- |
| `Research / Decision Result` | 研究或决策 | 问题、结论、证据时效和结果去向明确；未知不得伪装成立 |
| `Engineering Result` | 文档、CI、迁移、基础设施 | 约定工程结果和相称证据成立，并明确不代表用户能力 |
| `Component Delivered` | 产品或平台组件 | 组件、制品、局部 L2/L3 或定向 L4 成立，但完整运行时或用户结果尚未成立 |
| `Runtime Assembled` | 多组件运行时 | 真实入口 / 触发、实例化、调用方、状态与外部适配已装配；尚未必证明用户最终收到结果 |
| `User-path Delivered` | 用户或管理员能力 | 真实入口、完整处理、外部交付确认及用户可见成功 / 失败在指定层级成立 |
| `Release / Production Result` | 发布与生产 | 冻结制品已按授权发布、验证和可恢复 |

PR 合并、包内可 import、测试手工注入、内部状态 `succeeded` 或 adapter 被调用，均不能自动升级实际终点。

### 3.3 角色职责

角色是职责路由，不是固定的人或模型。

| 角色 | 负责什么 | 不拥有的权力 |
| --- | --- | --- |
| 产品负责人 | 决定用户结果、范围、优先级、权限、数据与授权终点；批准 Trace 版本 | 不替代理完成技术验收 |
| 规划者 | 完成可达性审计、Wave 0、拆分、计划步骤与全部合同设计；生成或修订具体 Trace | 不实施，不替产品负责人作决定，不把缺失合同留给编排者猜 |
| 编排者 | 接收获批 Trace；维护动态事实；只路由 Ready Step；冻结候选；汇总缺陷；推进交付、回写和收口 | 不从本模板补 Trace 缺项；不改目标、范围、证据标准或授权终点 |
| 实施者池 | 在唯一 Owner 的 Story / Task 范围内实现、自测和修复；留下可恢复交接 | 默认不改其他 Story，不自行合并、发布或扩大范围 |
| 验收者 | 规划期确认验收可执行；实施期准备资产；冻结后独立执行并解释证据 | 不修改产品代码，不放宽标准，不用 mock 冒充真实外部结果 |
| 审核者 | 对固定候选独立核对需求、实现、测试和证据；有无发现都留证 | 不直接修复 |

**运行映射（v15）：**方法只固定角色与独立性，不固定供应商、工具、主机或模型。每份 Trace 必填 `execution_adapter`、规划者、编排者、实施者池与唯一独立审核者的实际模型 / 推理档位及运行位置；实施者和审核者都是编排者的子代理，但审核者不得参与候选实现。模型或供应商切换只能在批次边界由产品负责人明示，不自动降级，也不改变步骤、证据和授权。

## 四、规划者生成 Execution Trace 的合同要求

### 4.1 确认来源、范围和实时能力

1. 读取当前有效的系统约束、`AGENTS.md`、产品合同、当前能力、长期决策、技术设计、验证与门禁、相关 Issue / PR 及适用外部规范；记录版本、核对时间和冲突处理。
2. 按物理机器和环境核对本次实际可用的 Agent、模型、并发、权限、受控环境、部署与真实外部回读能力；历史状态不能替代实时回读。
3. 建立带版本的共享事实包，只写短结论和权威链接，不复制整套文档。
4. 读取全部输入 Issue。`Ready` 只表示目标、范围、验收和授权已齐；`Pre-ready` 必须写明进入条件、证据和 Owner。规划阶段发现的新需求只登记，不直接判 Ready。

### 4.2 批准前可达性审计

三项审计缺一不可；不适用项写“`不适用 + 原因`”。

- **A. 输入 Issue 准入与关闭资格：**逐项写明自身完成标准、当前事实、由哪个 Step 补齐、所需证据、Ready 条件及关闭或保留去向。
- **B. 结果可达性：**对每个结果按链路核对：`真实入口 / 触发 → 运行时承接与调用方 → 核心处理和状态 → 外部副作用或交付 → 可观察成功 / 失败终态`。空实现、失败关闭桩、零调用方和未进入部署形态必须标为缺件。
- **C. 实现 / 验收双账：**每条工作线分别记录“还要实现什么”与“实现齐备后验什么”，分别估算窗口。实现量跨越批次时在批准前拆出 Implementation Epic，不得等到尾声把验收改判为开发。

### 4.3 Wave 0 研究与 Decision Gate

可能推翻架构、合同、权限 / 数据边界或关键估算，且无前置依赖、可无副作用研究的未知项进入 Wave 0：首波并行派发；依赖 Gate 的 Step 保持 Pre-ready；通过、不成立、未知和超时的去向全部预写；规范回源只证明 L1，真实系统才能回答的问题预留只读探针。需要改变产品或权限边界时进入 Decision Issue，由产品负责人选择。

### 4.4 拆分与计划执行步骤

规划者把 Ready 输入形成 Epic，拆成可观察 Story / Task，写清范围与明确不做、依赖与可并行、生命周期与终点、完成标准与关闭条件。多个 Epic 共同形成一个用户目标时，必须有最终 Integration / Release Epic 或 Trace 级最终验收。

计划执行步骤是交给编排者的核心产物。每个可派发单元使用稳定 `Step ID`，写明：Wave、所属层级、动作与可观察产物、启动条件、Owner 与主 / 备路线、资源租约、成功证据、成功后下一步、失败 / 未知 / 等待去向。至少一个 Step 在批准后可立即执行；细到能直接派发，不细到记录每条命令。

**关键角色主备路由**只为失效后阻塞关键路径、破坏独立性或无法取证的角色配置（编排、冻结审核、真实验收、观测通道、关键环境操作），对真要用的路径做最小冒烟；切换损害独立性、证据层级或权限边界时停止并升产品负责人。非关键机械工作写“备用不适用 + 原因”。

### 4.5 共享资源显式租约

对 Bridge、长连接、队列消费者、独占令牌、共享数据库、端口、测试机器人、受控环境等会互相影响的资源，Trace 记录：作用域、唯一 Owner、并发上限、开工前跨机器存量、取得条件、创建与释放点、精确清理方法、恢复路径；第二客户端启动前必须先证明唯一性；每次真实验收窗口后收工回读，Plan 收尾做残留盘点。**共享 Git 工作树同理：做变异实测或跑测试的并行代理必须使用独立 worktree，同一工作树同一时刻只允许一个写入者**（#203 事故实证）。

### 4.6 唯一可追溯验收对象与验收合同

每条正式验收 / 发布路线在批准前选定对象规则（PR 阶段冻结 artifact / 合并后由已验收树产生的不可变镜像 / 制品组 / 不适用+理由）；批准时确定制品类型、构建链、绑定与重验规则，实际 SHA / digest 冻结时回填；另一种制品不得静默当等价替代。

验收合同必须实例化以下机制（均有事故实证，来源见版本记录）：

- **真实链路首触冒烟前置：**Acceptance Epic Ready 之前，每条用户可见路径先完成一次非正式端到端真实冒烟；发现进入缺陷账本，不消耗正式候选。
- **用户可见断言双通道观测：**每条“用户看到什么”的断言预写两条独立观测通道（页面观察、平台 API 回读、服务端结构化日志任取其二），双通道一致才定论；冲突升产品负责人并登记证据对应性问题。
- **受控触发夹具确定性合同：**受控失败 / 安全 / 降级旅程必须有确定性触发机制，默认关闭、用后撤除并回读；夹具输入契约与生产解析同口径并有自身测试；夹具缺陷单列。
- **冻结前静态排除对账：**把已登记的范围排除 / deferred / 知情接受清单逐项对到本 Epic 验收断言上，命中者标缺件或取得产品负责人明示接受，不得静默滑过。

证据层级、真实外部风险、CI 范围与验证频率按《验证与门禁》（含金字塔频率纪律）选择并实例化到 Trace。

### 4.7 预测时间与停止条件

分别估算实现 / 装配、审核 / 修复、候选验收、外部等待和产品决定窗口。审核轮次和时间只是预测与预警，不是质量上限；已确认的 P1/P2 可在获批范围内完成必要修复。修复改变产品范围、权限、数据、不可逆动作或授权终点时，退回规划者形成 Trace amendment。关键阶段与关键等待以带时间戳的评论留痕；并行区间不相加冒充项目耗时。

### 4.8 写入、读回并取得批准

复制第七节模板创建 `[tracking]` Issue。每个必填字段只允许三种内容：具体值；`不适用 + 原因`；`Pre-ready + Owner + 转正证据`。通过真实换行的正文文件写入并读回核对。尚未决定的产品 / 权限 / 数据 / 外部边界显式列为待决事项。产品负责人批准明确版本后记录批准时间和证据，再交给编排者。

## 五、Ready 与交接门

Execution Trace 只有同时满足下列条件，才能由 `Planning` 进入 `Ready`：

- [ ] 所有输入 Issue 都有当前事实、Ready 条件和收尾去向；适用结果完成可达性检查；实施与验收已分账。
- [ ] 可能推翻合同或关键估算的无依赖未知已进入 Wave 0；依赖 Step 保持 Pre-ready。
- [ ] 至少一个 Step 可立即执行；每个 Step 有稳定 ID、启动条件、成功证据及成功 / 失败 / 等待分支。
- [ ] 每个 Epic 的生命周期、计划交付终点和完成 / 关闭条件明确。
- [ ] 规划者与编排者已分为独立顶层任务 / 会话；`execution_adapter`、角色模型映射、唯一独立审核者和批后继任条件已实例化；共享资源已有租约与唯一性检查。
- [ ] 每条正式验收 / 发布路线的对象规则明确；首触冒烟、双通道观测、夹具合同、静态排除对账可执行。
- [ ] 外部动作、不可逆副作用、恢复路径和授权终点明确；发现去向与收尾可执行。
- [ ] 正文已读回，方法版本、合同版本和产品负责人批准证据完整。

编排者只能检查上述内容是否存在和是否与批准版本一致，不能替规划者补写。

## 六、执行闭环：编排者纪律

下列规则必须实例化进具体 Trace 的步骤、验收或收尾合同；不是编排者在 Trace 缺项时直接从本 Issue 调用的后备合同。

### 6.1 接手与范围变化

先核对方法 / 合同版本、批准证据、基线、Ready Step、共享资源、制品规则和授权终点。只派发已 Ready 的 Step；Pre-ready 凭预写条件的证据转正。新 Issue 不自动纳入。合同缺项或需要改变目标、范围、证据标准、权限、外部动作时，停止受影响工作退回规划者；不受影响的 Ready Step 可继续。

### 6.2 主线灯塔与支线纪律

Trace 执行期内，任何派发 / 立项 / 修复先过判据：**「不做它，Trace 当前里程碑能不能达成？」**不能达成才进当前批次，其余进「里程碑后」队列默认不动。编排者维护「距里程碑清单」并逐次汇报余项数。新发现问题默认只登记——优先写代码旁模块文档「已知边界」；确需跟踪的并入既有登记 Issue，不新开、不散开。唯一例外：阻塞里程碑。

### 6.3 攒批与验证金字塔

修复攒批：批内多 commit、零中途完整门禁、零中途独立审查；批次终点一次完整门禁 + 由唯一独立审核者完成的一次批量审查，面向 `main` 串行合并。给产品负责人的裁定请求攒成统一清单一次提交。验证频率执行《验证与门禁》的**金字塔频率纪律**：层级越高成本越高、频率必须越低、单次覆盖的 commit 必须越多；批次收口评论报告「本批 Epic Full 次数 / 合入 commit 数」，比率退化按方法问题处理。仅生产级紧急修复、需独立回滚边界的高风险改动才单独走。

### 6.4 派发卡合同

派发卡固定包含以下条款（每条都有实测事故或损耗依据）：

1. **一路做到底：**按自己列的顺序做完再报，不逐步向编排者确认；只有需要产品取舍、扩大范围、或不同意编排者裁定时才停下。
2. **不得派发任何子代理。**
3. **长命令显式传足够长的超时参数**（本仓真库门禁约 5–7 分钟），不让命令转入后台再裸等通知。
4. **启动的验证必须等到结束并消化结果后才许收口**，不得「已启动」即停。
5. **报告格式含逐条处理状态**：已改 / 未改 + 原因——不同意编排者的裁定是允许的，但要给证据（实测 6 例否决 6 例都对）。
6. 公开可见内容标注与最小披露复核按《协作约定》。

编排者侧配套：subagent 报告「在等 X」时必须当场挂编排者自己的兜底观察，绝不裸等。

### 6.5 实施与验收资产并行

Story / Task 动态路由，同一 Story 同时只有一个 Owner。Story 自测、白盒测试和适用快检必须通过，但不为普通 Story 重复完整验收与全部镜像构建。验收者并行维护 CI、脚本、夹具、探针和用户旅程。批量变更、迁移、环境变异按 Trace 绑定的规则、验红和恢复路径处理。

### 6.6 候选冻结、审查与修复

1. Story 实现与快检、验收资产就绪后冻结 Epic，记录唯一候选并核对 `审核对象 = 冻结对象 = 正式验收对象`；冻结后不再向候选加入改动。
2. **每个固定候选只允许一名独立审核者。**该审核者可用多个视角核对需求、实现、测试、静态排除与合并面，但不得参与候选实现、不得直接修复；CI、实施者自查和收口事实核账都不是第二名独立审核者。
3. 审核发现由编排者裁定后进入唯一缺陷账本与一次统一修复包，实施者修复，仍由同一审核者定向复核；候选 SHA 改变后旧结论失效。接受不修的项必须写进代码或文档「已知边界」并说明证据。
4. **重构 / 搬移类审查固定项：**逐一核对按文件路径扫源码的守卫在实现搬走后扫的还是不是真正持有实现的文件；断言总数不减少**不构成**覆盖面未缩小的证据（实测：拆分后守卫变成永远绿的空断言）。**变异实测全程 `python -B` 或逐次清 `__pycache__`，不得以 mtime/size 判断源码已还原**——等字节替换＋同秒还原使字节码缓存判据失灵，红绿均不可信（批次5 与 rc22 两次实证）。
5. **并行分支 ≥ 2 且有共同文件面时**，审查阶段做一次实际合并演练（审查者 worktree 内，不推送），产出合并树门禁结论与关键清单 / 断言计数（实测两条「合起来才炸」）。
6. P1 在进入下一阶段前必须解决、缩小范围或经产品负责人修订合同。修复只路由受影响范围；新候选按影响重验。合入 `main`、发布镜像、运行时装配、部署、用户收到结果是不同结论，逐项声明。

### 6.7 动态事实与收口

- **批次收口评论**（带 UTC 时间戳）是动态事实的基本单位，必须包含：本批用户可见结果、绑定候选 / 制品标识、各 Step 状态与下一步、证据链接、**成员 Issue 回写（或明确「本批不涉及」）**。交接 = 一条自足的交接评论。
- **跨时区调度一律 UTC + 北京双标注。**
- **临时授权带失效时点：**追加授权必须原话留痕并写明失效时点（默认下一批次收口），到点失效不得滚动沿用（rc22 夜批实证）。
- **已知边界登记：**含频率判断（偶发 / 罕见 / 必现）的登记必须附观测依据，否则只写「原因未明」（实测：误登记「偶发」险掩确定性失败）。
- Story / Epic 达到终点后立即检查关闭资格。每个发现、事故和未验证边界必须进入且只进入一个有证据的去向：当前修复闭合；明确开放 Issue；产品负责人接受不处理；或重复 / 误报 / 不适用。
- **收口标准动作：**Trace / Epic 收口前并行核对逐 Issue 完成标准与仓库文档回写清单；实施者 / 事实核对子代理只做账目回读，不形成第二个候选审核结论。
- 长期产品、能力或决策变化先回写仓库正文；临时进程、容器、worktree、测试实例谁建谁清，长 Plan 收尾含逐台残留盘点。

### 6.8 顶层编排接力制与元守护（v16）

- 每份 Trace 选一个 `execution_adapter`：Codex 用全新顶层 Task；Claude Code 用**一个常驻 tmux session＋每批一个批次代号命名的 window**（终端只连一次，换班切窗口，退场窗口留供回翻）。适配器只改变启动 / 接管方式，不改变合同、证据、角色或留痕。
- **批次收口固定序列**：收口评论 → 自足交接评论（下一 Ready Step、候选 / 制品、缺陷、等待、租约、授权）→ 创建继任 window / 任务 → 继任读回三件套与最近交接并登记接管 → 写入 / 合并租约原子转移 → 旧编排者退场。有后续批次不得省略继任；最终批可不创建。
- **元守护**：常驻 session 的守望角色（规划者或上任编排者转任，合同指定），拉起各批编排者并**只以外部证据判活**（窗口存活＋留痕新鲜度，30~60 分钟一查）；不实施、不持合并权；承接产品负责人裁定并转发。**失联判定=窗口死亡且 >90 分钟无外部留痕**→先取证留痕再按最新交接拉继任。**编排者会话模型 / 形态偏离合同=异常信号**：先外部核账（合并面、部署面）再报产品负责人裁处，不采信会话自述。
- 任何时刻只有一个活动编排者和一个共享写入者；编排者不得清空上下文后冒充继任，顶层编排不得用不可找回的 headless 形态。供应商 / 适配器 / 模型切换只允许在批次边界经产品负责人明示；失联不触发自动切换。夜间有待裁项时仍立即派发不依赖它的 Ready Step，禁裸等。
- 证据：#328 接力试验；rc21 定义＋rc22 实践（#469 夜间异常由外部守望捕获，接力走通）。

## 七、状态、实际终点与 GitHub 关闭分开

### 7.1 Trace 执行阶段

`Planning → Ready → Executing → Closing → Closed`。冻结、修复与等待是 `Executing` 的子状态，在动态评论写明对象、原因和解除条件，不单列。

### 7.2 Trace 整体交付结果

| 结果 | 含义 | 关闭理由 |
| --- | --- | --- |
| `Undetermined` | 尚未到最终裁定 | 不关闭 |
| `Complete` | 获批范围全部达到终点 | `completed` |
| `Partially Delivered` | 产品负责人明确结束；部分达到终点，其余已逐项转交 | `not planned` |
| `Stopped` | 未形成批准的整体交付，已明确停止原因和去向 | `not planned` |
| `Superseded` | 已批准的新 Trace 完整接管剩余范围 | `not planned` |

`Closed + Partially Delivered` 只表示账目和环境已收口；未完成的实际工作 Issue 保持开放。

## 八、Execution Trace Issue 模板

v15 起，以下合同区结构实例化为 `docs/traces/<issue号>-<短名>/合同.md`（三件套载体，见第二节）；`[tracking]` Issue 正文只保留「给产品负责人」段与瘦指针。删除示例文字；所有不适用项写明原因。

~~~~markdown
# [tracking] YYYY-MM-DD 长期计划：一句话目标

## 给产品负责人

- 最终得到什么：
- 用户 / 管理员 / 工程系统会得到什么：
- 明确得不到什么：
- 需要批准的 Trace 版本、外部动作与授权终点：
- 当前待决定事项；没有则写“无”：

# 合同区：规划者生成，变化需重新批准

## Execution Trace 合同

- 方法版本：v15
- Trace 合同版本：v1 / v2 / …
- 执行阶段：Planning / Ready / Executing / Closing / Closed
- 整体交付结果：Undetermined / Complete / Partially Delivered / Stopped / Superseded
- 基线提交与核对时间：
- 目标与范围：
- 明确不做：
- 整体用户 / 工程结果与证据终点：
- 授权内动作与明确不授权动作：
- 停止条件与 Trace amendment 条件：
- 产品负责人批准时间、批准版本与证据：
- `execution_adapter`：`codex_new_task` / `claude_tmux_named_session`
- 规划者任务 / 会话与停止点：
- 编排者、实施者池、唯一独立审核者的模型 / 推理档位：
- 当前顶层编排任务 / 会话、主机、工作目录与版本快照：
- 编排者接手入口、第一恢复点与批后继任条件：

## 现场合同来源与共享事实包

- 来源核对（系统 / AGENTS.md / 产品合同 / 当前能力 / 决策 / 技术设计 / 门禁 / Issue / 外部规范）：版本、核对时间、冲突处理各一行
- 已生效决定与边界：
- 当前基线、模块、真实入口和关键依赖：
- 验收断言与权威链接：
- 仍未知但不阻塞当前 Ready 工作的事项：

## 开工能力与环境快照

| 机器 / 环境 | 实际可用 Agent / 模型 / 并发与权限 | 本 Trace 最小冒烟及证据 | 不可做 / 未知 | Owner |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

## 批准前可达性审计

### 输入 Issue 准入与关闭资格

| 输入 Issue | 自身完成标准 | 当前事实与证据 | 负责补齐的 Step | Ready 条件 | 收尾去向 |
| --- | --- | --- | --- | --- | --- |
| # |  |  | S- |  | 关闭 / 回写 / 保持开放 |

### 结果可达性

| 用户 / 系统结果 | 真实入口 / 触发 | 运行时承接与调用方 | 核心处理与状态 | 外部副作用 / 投递确认 | 成功 / 失败终态 | 当前缺件 | 负责 Step |
| --- | --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  | S- |

### Epic 交付、双账与窗口预测

| Epic | 类型 | 生命周期 | 计划终点 | 尚需实现 / 装配 | 实现齐备后验什么 | 依赖 | 实现 / 验收窗口预测与超时预警 | 完成与关闭条件 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| # |  |  |  |  |  |  |  |  |

## Wave 0 研究与 Decision Gate

| Gate ID | 待确认问题 | 无副作用研究 / 探针 | Owner | 所需证据 | 依赖它的 Step | 成立后 | 不成立 / 未知后 | 超时动作 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| G- |  |  |  | L1 / L4a | S- |  |  |  |

## 计划执行步骤

| Step ID | Wave | Epic / Story / Task | 动作与可观察产物 | 启动条件 | Owner / 主路线 / 备选路线 | 资源租约 | 成功证据 | 成功后下一步 | 失败 / 未知 / 等待去向 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| S- | W0 / W1 / … | # |  |  |  | R- / 无 |  |  |  |

> 关键角色的自动切换条件、独立性影响与必须请产品负责人的降级情形，写在对应 Step 的「失败 / 未知 / 等待去向」列或紧随其后的说明中；冒烟证据统一记在「开工能力与环境快照」。

## 共享资源租约与环境清理

| Lease ID / 资源 | 作用域与并发上限 | 唯一 Owner / 开工前存量 | 创建与释放点 | 停止 / 精确清理 | 恢复路径 | 收工证据 |
| --- | --- | --- | --- | --- | --- | --- |
| R- |  |  |  |  |  |  |

## 正式验收 / 发布对象合同

| Epic / 路线 | 对象类型与构建链 | 绑定方式 | 冻结时回填 | 替换后的失效与重验 | 回滚对象 |
| --- | --- | --- | --- | --- | --- |
| # | PR artifact / 合并后镜像 / 制品组 / 不适用 |  | SHA / tree / digest |  |  |

## 授权与外部动作

| 动作 / 环境 | 是否在授权内 | 执行者与前置证据 | 副作用与幂等边界 | 恢复 / 回滚路径 | 完成回读 |
| --- | --- | --- | --- | --- | --- |
| 合并 / Stage / 发布 / 数据变更 / 外部发送 / 生产 | 是 / 否 / 需再次确认 |  |  |  |  |

## 验收、审核与修复合同

- Story Fast / Epic Full / Main Publish 适用范围与金字塔频率实例化：
- 每个 Epic 的验收断言、证据层级和环境：
- 每条用户可见路径的首触冒烟安排：
- 用户可见断言的双通道观测规则：
- 受控触发夹具清单：
- 固定候选的唯一独立审核者、独立性、对象与静态排除对账结论：
- 单一审核结论、唯一缺陷账本、收敛线与同一审核者定向复核安排：
- 候选冻结、唯一缺陷账本、统一修复、重新冻结和定向复验规则：
- 审核 / 修复预测与超出预警；循环退出条件：
- 每批收口、继任顶层任务 / 会话创建、接管登记与租约转移规则：

## Ready 与交接自检

- [ ] 第五节交接门逐项满足
- [ ] 正文已读回，合同版本和产品负责人批准证据完整
- [ ] 规划者已停止实施；独立顶层编排任务 / 会话、运行映射与批后继任条件已明确

~~~~

## 九、记录、修订与关闭边界

- 本 Issue 只维护方法正文与版本记录，不承载具体业务进度。评论只记录讨论、复盘证据和下一版候选；未回写正文的评论不改变当前方法。
- 已批准 Trace 固定使用其记录的方法版本；模板升级不自动改变正在执行的 Trace。
- 系统指令、`AGENTS.md`、权限 / 安全边界和仓库强制规则不因 Trace 漏列而失效。
- **日落条款（v13 起）：**本文每条机制条款必须可追溯到证据（复盘或事故链接，见版本记录）。每次 Trace 收口复盘固定做一次**反向审计**：上一 Trace 中一次都没被引用或填写的机制，列为下一版删除候选。模板修订默认净减法：新增条款须同时提名删除候选，正文体量不得超过上一版（产品负责人 2026-08-24 规则）。
- 当前方法 Issue 长期保持打开；被后续版本替代时先迁移唯一入口，再标为历史。

## 十、版本记录

- **v16（2026-08-31）**：产品负责人授权按 [rc22 复盘](https://github.com/Moshuiwang/lingxi/issues/469#issuecomment-5474257188)升级：§6.8 常驻 session＋批次 window＋元守护；§6.7 临时授权失效时点；§6.6 变异验红硬姿势。
- **v15（2026-08-30）**：规划 / 编排分会话、供应商中立适配、单一独立审核、批后接力；删 `/cost` 与 Fable/tmux 全局默认。
- **v14（2026-08-28）**：三件套、会话接力与编排者模型字段并入（[#328 复盘](https://github.com/Moshuiwang/lingxi/issues/328#issuecomment-5447228230)）。
- **v13（2026-08-24）**：#203 复盘的派发、攒批、审查、主线、收口核账与金字塔频率并入；删未维护动态表。
- **v12 / v11（历史）**：v12 增首触冒烟、双通道、夹具合同、静态排除对账（[#162 复盘](https://github.com/Moshuiwang/lingxi/issues/162#issuecomment-5308799076)）；v11 增可达性审计、Wave 0、Step ID、终点分账、租约（[#104 复盘](https://github.com/Moshuiwang/lingxi/issues/104#issuecomment-5277196628)）。正文见编辑历史。
- **v10（历史）**：建立四层工作、两层合同、分层 CI、固定候选、统一缺陷账本；正文见 [Issue #83](https://github.com/Moshuiwang/lingxi/issues/83)。
- **v9（历史）**：见 [Issue #59](https://github.com/Moshuiwang/lingxi/issues/59)。




