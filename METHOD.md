<!-- trace-kit：执行方法正文。本文件为源 Issue 的原样搬运，正文一字未改；修订只在源 Issue 进行，再按版本同步到这里。 -->

> **trace-kit 版本头**：方法版本 **v20 r1**（2026-09-13）。源 = GitHub lingxi Issue [#678「长期执行计划与 Execution Trace」](https://github.com/Moshuiwang/lingxi/issues/678)（现行唯一正文与修订入口；旧源 [#147](https://github.com/Moshuiwang/lingxi/issues/147) 已降为历史正文，只保留 v17 及更早）。搬运时间 2026-09-13（源 Issue 最后更新 2026-09-13T14:08:14Z）。正文中的「本 Issue」「评论」指源 Issue；「仓库 `docs/traces/`」指采用本方法的项目仓库；「验证与门禁」「协作约定」指该项目的对应文档（骨架见本套件 `template/docs/`）。

# 长期执行计划与 Execution Trace 模板 v20

> **状态：现行唯一方法入口，v20 r1（2026-09-13）。**沿用 v9→v20 编号；来源是 [#147 的 v17 历史正文](https://github.com/Moshuiwang/lingxi/issues/147)、[#665 已批准的 v18 第 1–8 项](https://github.com/Moshuiwang/lingxi/issues/665#issuecomment-5567200170)、v19 的运行面通用化，以及 v20 按三批复盘（#709 / #732 / #754）做的先删后增。**模板不绑定单一执行者**：方法正文对任何执行者成立，运行面差异见第四节。
>
> **阅读对象：规划者。**本模板不能直接授权执行。规划者据此生成具体 `[tracking]` Execution Trace；产品负责人批准明确版本后，交给独立的顶层编排任务。编排者只按获批 Trace 和其中链接的来源推进，不能从本模板、聊天或历史评论补范围与授权。
>
> **运行面由具体 Trace 绑定**，在开工快照里写明执行者、执行机、操作系统与时钟基准。远端研发、受控验收、外审和发布按具体 Trace 接入。已经批准的历史 Trace 继续使用其绑定版本，不自动迁移。

## TO PM

具体 Trace 只写这次工作会变化的结果、范围、步骤、决定、证据和恢复点。项目固定规则、执行者的工具教程以及 GitHub 已有记录不再重复抄写。产品负责人一次批准可覆盖到明确终点；新增权限、数据范围、不可逆动作或未获批外部写入仍须另行决定。

## 一、什么时候需要 Trace

满足多 Epic、多批次、顺序依赖、跨任务接力，或涉及部署、权限、数据、真实外部系统时使用 Trace。单一低风险 Story / Task 直接使用原 Issue。

具体 Trace 必须选择一个授权终点，例如：仅提交、分支已推送、PR 可合并、已合并、不可变候选已形成、stage 已部署、stage 已验收或生产已发布。达到前一项不能自动声称后一项成立；模板本身不授予 stage 或生产写入权限。

### 角色

| 角色 | 职责 |
| --- | --- |
| 产品负责人 | 决定结果、范围、优先级、权限、数据和授权终点；批准 Trace 与必要变更 |
| 规划者 | 核事实与可达性，形成最小合同；批准后停止实施，可转任守望者。转任只转移只读监护，不授予预算裁定、拉起编排者等权限，那些由合同或产品负责人另批 |
| 守望者 | 只以外部证据判活、承接并转发产品负责人裁定、失联时取证后拉继任；不实施、不持合并权。程序见 `trace-kit` 的 guardian |
| 编排者 | 只派 Ready Step，维护恢复点，冻结候选，组织审核、验收和收口；不补合同 |
| 实施者 | 在分配范围内实现、自测和修复；不扩大范围 |
| 验收者 | 对约定对象执行验收并解释证据；不修改产品代码 |
| 审核者 | 对固定候选独立判断；不参与实现，不直接修复 |

Step 的 Owner、候选审核记录、PR / commit / run 链接和交接快照构成主链记录，不建立逐动作角色流水账。

## 二、正文分层与载体

| 层 | 写什么 |
| --- | --- |
| 固定规则 | 本 Issue、仓库 `AGENTS.md` 和验证与门禁；具体 Trace 不复制 |
| Trace 合同 | 本次结果、范围、授权终点、步骤、Gate，以及命中的例外 |
| 运行事实 | 当前状态、候选、证据、等待与清理；只在状态变化、决策、批次收口或交接时更新 |

每个 Trace 使用 `docs/traces/<Issue号>-<短名>/`：

- `合同.md` 必需：最小合同与步骤；建立或改变范围、授权、验收标准时走 PR，按仓库批准规则生效。
- `任务表.md` 必需：Step 状态和三行恢复点；不复制合同全文。
- `验收.md` 按需：只有存在 Trace 级新增断言、跨 Epic 总验收或需要集中证据索引时创建；否则直接链接成员 Issue / PR 的验收。
- `[tracking]` Issue 只写目标、文件链接和阶段；评论承载产品裁定、批次收口与交接。

合同硬上限 **40KB**，超出即拆：把「现场合同来源」「开工能力与环境快照」「批准前可达性审计」三张表移入同目录的 `开工快照.md`，合同正文只留六段、计划执行步骤、覆盖对照表与裁定清单。不得把授权与验收门槛移出合同来绕过批准。已有 Trace 不强制改写。

## 三、规划者只回答必要问题

1. **结果：**产品负责人最终得到什么，唯一授权终点是什么，怎样证明达到。
2. **范围：**输入 Issue、明确不做、会影响用户承诺的边界及关闭去向。
3. **步骤：**每个 Step 的可观察产物、Ready 条件、Owner / 工作区、通过证据、成功后继和失败 / 等待去向。
4. **触发项：**外部写、共享资源、跨任务、独立审核、特殊预算分别写有 / 无；只有“有”才展开。
5. **批准：**至少一个 Step 可立即 Ready；未知项有 Owner 与补齐条件，受它影响的 Step 保持 Pre-ready。

依赖必须能说明传递的制品，或占用的独占资源 / 额度；否则删除。多个 Epic 共同形成一个用户结果时，保留最终集成、验收或发布 Step。规划期核前提可派只读子代理并行批量做（不改文件、不写外部系统），人次与 token 在预算里单列。

### 人类 Gate 与预算

目标、范围、产品承诺、权限、数据、不可逆动作和外部写入必须在受影响 Step 前获批。已知且可逆的动作可以预授权，但须写触发条件、批准动作、有效期和不回复时的停止位置；沉默不能产生新授权。不阻塞里程碑的建议留到收口集中处理。

默认每个固定候选只做一次适用的完整门禁和一次约定的独立审核；真实验收只在约定窗口执行。Trace 写预计批次、候选 / 重检查次数、外审或 stage 窗口，以及明确的时间或费用上限。**规划期给量级预估（子代理人次、token 量级、PR 数、CI 次数），收口评论逐项对账**——只给上限不给预估，事后无法判断上限定得准不准。**PR 次数按批次数 + 2 估**，超出需说明；每个 PR 至少消耗一次完整检查。**真人验收是缺陷发现口，不只是取证动作**：候选预算须写明真人验收打回时重做几轮完整门禁与重新部署，收口对账写「因打回重做的轮次：预估 / 实际」。到线时完成当前安全单元、保存证据、释放资源，再请求预算或范围调整，不能靠跳过失败或降低证据等级省预算。

**规划前先核本机能力并写进开工快照**：当前负责编码的主代理是什么、用的语言模型是什么、剩余额度多少、何时重置；可调用的外部审核是什么、用什么模型、额度多少、怎么调用——**怎么调用这一条规划者必须实机测试一次并记录调用姿势与结果**，不得凭记忆或文档声称可用。外审频率按额度定：额度充裕时按批次甚至按候选频繁外审，额度紧张时只在最终 PR 前调用一次；频率与额度写进预算段，收口对账。

### 命中才展开的合同

- **共享资源：**只记录实际会冲突的数据库、机器人、端口、队列、令牌、stage、合并道或工作树；写唯一 Owner、取得条件、释放与清理。同一工作树同一时刻只有一个写入者；并行变异或重检查必须隔离目录和依赖；两条并行分支改到共同文件时，在独立工作树先做合并演练。
- **外部写与发布：**写目标、获批动作、固定制品、恢复方法和回读证据。审核对象、验收对象与发布对象必须对应；候选 SHA / digest 变化后，旧结论按影响失效。**候选冻结后产生的纯记录提交不推回该 PR**——推了 PR 头就离开被验收的候选，且按整体差异重跑完整门禁；合并后另走文档 PR，收口时 PR 头 SHA 须逐字等于验收单里的候选 SHA。
- **真实验收：**只写本次断言、证据层级和实际观测入口。页面可见性与平台客观状态分别取证；测试替身、内部成功状态或绿色 CI 不能冒充真实用户结果。**每条判据写明被判的值由谁写入**：判定字段只认系统写出的值，前置状态允许人工构造；回答不了「这个值是谁写的」的判据无效。
- **代价陈述的事实基础：**合同与工作项里「不应用的代价」「现在做不到什么」这类陈述必须回源到直接证据，不得由相邻组件的状态推导。相邻不等于同一条链路——某个组件未启用，推不出它所在的那类能力不存在。写不出直接证据时如实写「未知」与取证方法。
- **实测用例的副作用：**任何将在真实环境执行的用例，写完后必须逐步回源核对其执行分支是否含不可逆操作（硬删除、外部发送、权限变更、不可回滚迁移）。命中即改用可逆路径，或停止并上报。**「有还原步骤」不等于可逆**——还原只覆盖它自己那一步，不覆盖同一分支里的其他副作用。**转抄核对必须核语义，不能只核字面**（格式、列数、指纹一致不构成安全性证据）。
- **首触冒烟：**正式验收前，在安全且可恢复时先走一次非正式真实路径；没有可用注入口时并入第一条正式用例，失败进入缺陷处理，不把模拟重放当成已部署冒烟。
- **主备路线：**只有主路线失效会阻塞且备用路线真实可用时才写；切换会改变权限、独立性或证据时停止并请产品负责人决定。

## 四、编排方式

默认由一个顶层编排任务持续推进，独立且有边界的实施、调查或验收可交子代理。启用任何常驻机制（后台任务、定时巡检、跨会话接力）须有产品负责人的明确请求或已批准 Trace 授权。

下面第一段是与执行者无关的通用纪律，其后是 Claude Code 运行面的原语；绑定其他运行面的 Trace 在合同里自带原语表（Codex Desktop 原语见 v19 正文与本 Issue 历史评论）。

### 通用编排纪律

**守望者的值守必须落成机制，机制本身要能被查。**交互式会话没有自己的计时器，「每 N 分钟查一次」写进合同不产生任何行为。守望者须同时挂两类机制，缺一即欠覆盖：**事件型**管「死没死」（常驻轮询，只用本地证据：窗口输出、工作树提交；查询失败须显式重试并声明）；**定时型**管「做到哪」（按周期给守望者一个完整回合，查外部留痕并对照任务表）。两类机制都是会话级，窗口关闭即失效，合同须写明失效后由谁接手；定时型自带收尾条件。守望者不得声称任何未被机制支撑的巡检频率，声称时给出机制标识与启动证据。**机制上线前，先对机制本身跑一遍下面的有效性判据。**

**兜底观察的有效性判据**（守望机制与任何外部等待的观察哨同样适用）：循环条件必须能区分「目标达成」与「查询本身失败」——只判断「输出里没有进行中标记」不成立，查询失败的错误输出里同样没有那个标记。观察脚本必须显式校验响应可解析，不可解析时继续重试，并覆盖成功、失败与超时三种结局。**「挂了观察哨」不等于「观察哨会叫醒我」**，判活只信外部证据。

### Claude Code 运行面

编排原语与角色流程由 `trace-kit` 插件承载（开工合同、派发卡、守望者、交接、接管、看板），**模板不重复其正文**；Trace 只写本次绑定的角色与授权终点。要点：子代理由主代理派发且不得再下派，完成时主代理收到通知；跨会话接力用 tmux 拉起窗口与判活，**会话之间的转达一律走运行时的会话间消息**（有投递回执），向对方窗口注入按键不构成「已转达」，会话消息不是留痕；同一时刻只允许一个写入者；**后台任务与观察脚本的运行环境可能不具备网络访问**，需要查询外部状态的观察哨必须选用具备网络的机制并按上面的有效性判据自检。具体工具参数以当时的运行时说明为准，不写进 Trace。

### 派发、审核与验证

派发只需要六项：Step、可观察产物、范围、Owner / 工作区、通过证据、失败去向。**单个子代理任务预计超过 60 分钟即必须切分**；派发前给出预计时长，超时按兜底观察处置——长任务会阻塞编排者会话本身，期间编排者收不到任何新消息。授权内工作一路做完并等检查终态；需要产品取舍、扩大范围或新增外部副作用时停止。新发现只有阻塞当前里程碑才进入当前批，其余登记到既有载体。

每个固定候选由一名未参与实现的审核者审查；实施者修复，同一审核者定向复核。

**假证据六型（每个固定候选必查）**：①测试替身硬写被测判据，绕开真实规则表；②守卫按路径扫源码，实现搬走后扫的不是持有实现的文件；③变异锚点指向已删符号；④契约测试被条件跳过；⑤测试名、docstring 或验收断言里的无证据陈述（含「已实测」「已复核」类字样）；⑥断言与实现相反。**判据**：删掉被验证的那一行，测试仍全绿 ⇒ 该用例证明的是它自己注入的假设；断言总数不减少不构成覆盖面证据。候选变化后按影响重验。红色结果须保留并诊断，不能反复重跑到绿后丢弃。

本机检查只证明实际跑过的范围；与执行机操作系统或架构相关的进程、权限、cgroup、目标架构镜像与生产恢复转到 CI、研发环境或受控环境。`scripts/dev/check.sh full` 只对应完整 CI 的主 gate，不代表 extras、镜像或 stage 已通过。重检查默认串行；只有目录、数据库、端口和安装目录隔离并实测有收益时才并行。

独立外审的通道与调用姿势以开工快照里的实机测试为准（第三节）；一名审核者即可，不默认两边各跑一轮。正式审核必须能读取完整固定候选，记录发现、证据、影响和未验证范围。通道小样只证明可调用，不证明仓库候选已通过。

## 五、状态、恢复与收口

Step 使用 `Pre-ready / Ready / Running / Waiting / Done`。编排者只派 Ready；合同缺项或范围、授权、验收标准变化时，停止受影响 Step 并退回规划者修订，不受影响的 Ready Step 可继续。

CI、远端命令、部署和队列必须回读实际对象的终态；处于等待时记录对象、观察入口和失败去向，不能因“已发起”判定完成。**门禁的耗时余量是可观察量**：每次完整门禁记录最长单步耗时与其超时上限之比，收口对账列出；余量低于两成先调上限再推下一轮，不等它撞线。时间记录使用系统实取的日期、时区和起止点，不把并行耗时相加。

`任务表.md` 顶部始终保留三行：

- **当前：**阶段、Step、固定候选和最后一条通过证据。
- **下一步：**唯一下一 Ready Step、Owner 和入口。
- **等待 / 占用：**阻塞条件、决定人，以及仍持有资源和清理方法；没有则写“无”。

跨任务接力时，继任者先读合同与恢复点并确认接管；旧编排者确认停止写入、释放租约后退场，不能出现两个写入者。任务消息没有原子锁，未确认转移前保持等待。

批次收口只记录：达到的结果、候选与检查、剩余 / 下一步、待决定项、资源清理。主链从 Step Owner、PR / commit / CI、审核记录和交接快照复盘，不重复逐命令日志。

最终 `Complete` 只表示全部获批范围达到唯一授权终点。`Partially Delivered / Stopped / Superseded` 须由产品负责人决定，并给每项未完成工作一个明确去向。谁创建临时进程、容器、工作树、测试实例或自动化，谁在收口前清理并回读；需要保留时写 Owner、原因和清理方法。

## 六、Ready 自检

- [ ] 结果、范围、不做清单、唯一授权终点和批准版本明确。
- [ ] 至少一个 Step Ready；其他 Step 的进入、成功和失败 / 等待去向明确。
- [ ] 人类 Gate 已前置或预授权；关键未知没有被写成 Ready。
- [ ] 命中的共享资源、外部写、候选、验收、审核与恢复可执行。
- [ ] **新写的启动条件、完成条件与清零判据已对照它所依赖的对象集合自检**：不自指、不与被它依赖的对象互相等待。判据里点名的集合若包含本单自身或包含「要等本单完成才能收口」的对象，即为死结，须显式写排除项。
- [ ] **开工快照写明执行机的时钟基准与时区**；面向产品负责人的时刻一律双标注且两个值都实读，跨夜任务核日期不只核时分。
- [ ] **开工快照写明主编码代理与模型、额度与重置时点，以及外审通道、模型、额度与实机调用测试记录**；外审频率已按额度写进预算段。
- [ ] 编排者从合同与任务表能回答：现在做什么、谁做、何时可做、怎样算过、失败去哪、何时必须停。

## 七、可复制的最小结构

`[tracking]` Issue 只放目标、文件链接和阶段。以下分别写入 `合同.md` 与 `任务表.md`；未命中的附加项不建空表。

```markdown
# Trace：一句话结果

## 给产品负责人
- 结果与授权终点：
- 范围 / 不做：
- 待决定或已预授权的 Gate：无 / G-…
- 方法：#678 v20；Trace 版本、批准时间与证据：
- 输入、基线与完成标准链接：
- 触发：外部写=无；共享资源=无；跨任务=无；独立审核=无；特殊预算=无

## 执行步骤
| ID | 产物 | Ready 条件 / Gate | Owner / 工作区 | 通过证据 | 后继 / 失败去向 |
| --- | --- | --- | --- | --- | --- |

## 命中才展开
- 外部写与恢复：
- 共享资源与释放：
- 独立审核 / 跨任务接力 / 特殊预算：

# 任务表
- 当前：
- 下一步：
- 等待 / 占用：无
- [ ] S-… 状态｜一句话｜关键证据链接

# 收口
- 达到的终点与证据；未完成去向；清理结果：
```

## 八、版本与来源

**修订规则（v20 起）**：删除侧以「连续两批零引用，且由不受该条款影响的人复核」为判据；新增每条须点名一处删除并附事故或实测出处；「方法已覆盖、只是执行没照做」的现象只进 Trace 收口，不进正文。

- **v20 r1，2026-09-13：**产品负责人追加「规划前核本机能力与额度」条款（第三节预算段）与 Ready 门一项（第六节）：主编码代理 / 模型 / 额度 / 重置时点，外审通道 / 模型 / 额度 / 调用姿势须实机测试，外审频率按额度定。对应删除：第四节外审一句去掉具体工具名，改指开工快照。实证：#754 外审三次中两次因令牌失效静默零产出、另一工具三处用法错被拦，均为通道未实测即派发所致（[#754 复盘](https://github.com/Moshuiwang/lingxi/issues/678#issuecomment-5649964089)、[陷阱登记](https://github.com/Moshuiwang/lingxi/issues/754#issuecomment-5635839712)）。外部代理按额度分档参与实施的试点另见 [#769](https://github.com/Moshuiwang/lingxi/issues/769)，有数据后再议是否入正文。trace-kit `METHOD.md` 随本次同步。
- **v20，2026-09-13：**按 [#709](https://github.com/Moshuiwang/lingxi/issues/678#issuecomment-5615671481)、[#732](https://github.com/Moshuiwang/lingxi/issues/678#issuecomment-5631671335)、[#754](https://github.com/Moshuiwang/lingxi/issues/678#issuecomment-5649964089) 三批复盘先删后增，逐条变更表见 [v20 草案评论](https://github.com/Moshuiwang/lingxi/issues/678#issuecomment-5651545771)，产品负责人 2026-09-13 批准「按草案发布」。**删 / 并**：Codex Desktop 运行面整节（三批零引用；产品负责人确认 codex 只作外审与验证执行者，不再编排）、执行图、合同三档体量并为 40KB 一档、「并行分支先合并演练」并入共享资源条、「断言总数不减少」并入假证据判据句、末段 Codex 表述中性化、[三处残留](https://github.com/Moshuiwang/lingxi/issues/678#issuecomment-5616350267)清零。**改**：规划者「转为只读监护者」改为「转任守望者」并指向 guardian（#732 / #754 真用上），角色表加守望者一行；「元守护」统一改「守望者」（2026-09-09 裁定）。**增（各对应一处删除）**：守望者两类机制与上线前自检；真人验收打回留轮次；判据分「判定字段」与「前置状态」；冻结后记录提交不推回该 PR；门禁耗时余量作可观察量；转达走会话消息；规划期只读子代理批量核前提。**继续保持候选**：[#665](https://github.com/Moshuiwang/lingxi/issues/665) 第 9 项（K / N / O）仍无新证据；「会话在主检出拉起、隔离工作树不放系统临时目录」只有 2026-09-13 一次观察，先写进 2.5.0 合同；「产品负责人键入代理窗口的裁定以 Issue 回执为准」归 guardian / handoff 程序。
- **v19，2026-09-09：**模板不再绑定单一执行者（第四节拆为通用纪律 + 两个运行面）；新增三条：兜底观察的有效性判据、判据自指与循环自检（Ready 门）、代价陈述的事实基础。三条实证来自一次 Claude Code 编排会话而非正式 Trace 复盘；本版为净增，删除侧欠账登记到下一版（已由 v20 结清）。
- **r1–r3，2026-09-08/09：**r1 纳入 #665 已批准 1–8 项并完成 Codex 适配；r1.1 本 Issue 升为唯一入口、#147 降为历史；r2 按[第二轮精简审查](https://github.com/Moshuiwang/lingxi/issues/678#issuecomment-5588694204)净减 59%；r3 按 [#665 结果核对](https://github.com/Moshuiwang/lingxi/issues/678#issuecomment-5596074312)补回候选 B / M / A 六型枚举 / F 入 Ready 门 / L 40KB 硬上限 / J PR 上限与用量对账。
- **v17 及更早版本：**见 [#147](https://github.com/Moshuiwang/lingxi/issues/147)。

执行者能力以其运行时工具说明与官方文档为准；仓库验证以[验证与门禁](https://github.com/Moshuiwang/lingxi/blob/main/docs/技术设计/验证与门禁.md)及[本机检查说明](https://github.com/Moshuiwang/lingxi/blob/main/scripts/dev/README.md)为准。能力核验与一次性证据留在本 Issue 评论，不进入模板正文。


