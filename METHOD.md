<!-- trace-kit：执行方法正文。本文件为源 Issue 的原样搬运，正文一字未改；修订只在源 Issue 进行，再按版本同步到这里。 -->

> **trace-kit 版本头**：方法版本 **v23**（2026-09-20）。源 = GitHub lingxi Issue [#678「长期执行计划与 Execution Trace」](https://github.com/Moshuiwang/lingxi/issues/678)（现行唯一正文与修订入口；旧源 [#147](https://github.com/Moshuiwang/lingxi/issues/147) 已降为历史正文，只保留 v17 及更早）。搬运时间 2026-09-20（源 Issue 本次替换后读回逐字一致；变更表见修订 Issue [#842](https://github.com/Moshuiwang/lingxi/issues/842)）。正文中的「本 Issue」「评论」指源 Issue；「仓库 `docs/traces/`」指采用本方法的项目仓库；「验证与门禁」「协作约定」指该项目的对应文档（骨架见本套件 `template/docs/`）。

# 长期执行计划与 Execution Trace 模板 v23

> **状态：现行唯一方法入口，v23（2026-09-20）。**沿用 v9→v23 编号；来源与修订史见第八节。**本模板只供 Claude Code 使用**：规划、编排、实施、审核与守望均由 Claude Code 会话及其子代理承担；外部审查工具（某个 Trace 启用时）只做只读核实，绑定方式见第三节。
>
> **阅读对象：规划者。**本模板不能直接授权执行。规划者据此生成具体 `[tracking]` Execution Trace；产品负责人批准明确版本后，交给独立的顶层编排任务。编排者只按获批 Trace 和其中链接的来源推进，不能从本模板、聊天或历史评论补范围与授权。
>
> **运行面由具体 Trace 绑定**，在开工快照里写明执行机、操作系统与时钟基准、会话模型与档位、额度与重置时点。远端研发、受控验收、外审和发布按具体 Trace 接入。已经批准的历史 Trace 继续使用其绑定版本，不自动迁移。

## TO PM

具体 Trace 只写这次工作会变化的结果、范围、步骤、决定、证据和恢复点。项目固定规则、Claude Code 的工具教程以及 GitHub 已有记录不再重复抄写。产品负责人一次批准可覆盖到明确终点；新增权限、数据范围、不可逆动作或未获批外部写入仍须另行决定。

## 一、什么时候需要 Trace

满足多 Epic、多批次、顺序依赖、跨任务接力，或涉及部署、权限、数据、真实外部系统时使用 Trace。单一低风险 Story / Task 直接使用原 Issue。

具体 Trace 必须选择一个授权终点，例如：仅提交、分支已推送、PR 可合并、已合并、不可变候选已形成、stage 已部署、stage 已验收或生产已发布。达到前一项不能自动声称后一项成立；模板本身不授予 stage 或生产写入权限。

### 角色

| 角色 | 职责 |
| --- | --- |
| 产品负责人 | 决定结果、范围、优先级、权限、数据和授权终点；批准 Trace 与必要变更；离线前可给出预批裁定与关闭条件 |
| 规划者 | 先做里程碑分诊，再核事实与可达性、形成最小合同；开工前结清上一 Trace 的方法候选（起草下一版或写明不修订的理由）；Trace 收口时写合同对账复盘。批准后停止实施，可转任守望者。转任只转移只读监护，不授予预算裁定、拉起编排者等权限，那些由合同或产品负责人另批 |
| 守望者 | 只以外部证据判活、按固定五项汇报进度与堵点、承接并转发产品负责人裁定、失联时取证后拉继任、核编排者退场序列（含复盘评论）；退场前写机制对账复盘，并把各批次复盘的方法候选归档到修订 Issue；不实施、不持合并权。程序见 `trace-kit` 的 guardian |
| 编排者 | 只派 Ready Step，Ready 的默认并行派出，维护恢复点，冻结候选，组织审核、验收和收口；每个批次收口写复盘评论（规则见第五节）；不补合同 |
| 实施者 | 在分配范围内实现、自测和修复；不扩大范围 |
| 验收者 | 对约定对象执行验收并解释证据；不修改产品代码 |
| 审核者 | 对固定候选独立判断；不参与实现，不直接修复 |


## 二、正文分层与载体

每个 Trace 使用 `docs/traces/<Issue号>-<短名>/`：

- `合同.md` 必需：最小合同与步骤；建立或改变范围、授权、验收标准时走 PR，按仓库批准规则生效。
- `任务表.md` 必需：Step 状态和三行恢复点；不复制合同全文。
- `验收.md` 按需：只有存在 Trace 级新增断言、跨 Epic 总验收或需要集中证据索引时创建；否则直接链接成员 Issue / PR 的验收。
- `[tracking]` Issue 只写目标、文件链接和阶段；评论承载产品裁定、批次收口与交接。

合同硬上限 **40KB**，超出即把能力 / 环境快照与可达性审计移入同目录的 `开工快照.md`；授权与验收门槛不得移出合同。

## 三、规划者只回答必要问题

### 里程碑分诊（立项前）

规划者起草合同前先把目标里程碑内每张 Issue 整理到位，四项齐才算分诊完成：

1. **事实**：现状、证据与未知项已核过并写回正文；已生效的裁定整理进正文，不让编排者从评论链猜。
2. **验收标准**：可观察，写明被判的值由谁写入与证据层级。
3. **标签**：类型、状态、优先级齐全，命中的风险标签已打；状态按仓库状态链如实，前置未齐保持 `pre-ready`。
4. **里程碑归属**：逐张判「留 / 移出 / 顺延」并写理由；再反向扫其他里程碑与无里程碑的 open Issue（含 P0 / P1 无里程碑的），判「提前并入 / 不动」。

产出一张分诊表进开工快照（Issue、四项状态、建议、理由）；改标签、改里程碑、改正文的写动作经产品负责人裁定后执行。四项未齐的 Issue 不进 Ready Step，保持 Pre-ready 并写补齐条件与 Owner。分诊可派只读子代理并行做。

### 五问

1. **结果：**产品负责人最终得到什么，唯一授权终点是什么，怎样证明达到。
2. **范围：**输入 Issue、明确不做、会影响用户承诺的边界及关闭去向。
3. **步骤：**每个 Step 的可观察产物、Ready 条件、Owner / 工作区、并行组与文件边界、通过证据、成功后继和失败 / 等待去向。
4. **触发项：**外部写、共享资源、跨任务、独立审核、特殊预算、首次真机分别写有 / 无；只有“有”才展开。
5. **批准：**至少一个 Step 可立即 Ready；未知项有 Owner 与补齐条件，受它影响的 Step 保持 Pre-ready。

依赖必须能说明传递的制品，或占用的独占资源 / 额度；否则删除。**需产品负责人单独批准的 PR 不进批次终点的依赖，写成并行链。**规划期核前提可派只读子代理并行批量做（不改文件、不写外部系统），人次与 token 在预算里单列。

### 人类 Gate 与预算

目标、范围、产品承诺、权限、数据、不可逆动作和外部写入必须在受影响 Step 前获批。已知且可逆的动作可以预授权，但须写触发条件、批准动作、有效期和不回复时的停止位置；沉默不能产生新授权。不阻塞里程碑的建议留到收口集中处理。产品负责人触点排在其在线时段、能合并的合并为一次；每次触点先出就绪自检，待办与讨论分开置顶、一次一条。**给产品负责人的每条指令 = 可复制命令 + 逐字预期输出 + 停止条件，一次贴齐；生产写命令由产品负责人在守望者会话亲跑，输出直接落该会话当场核对，不经转贴。**

默认每个固定候选只做一次适用的完整门禁和一次约定的独立审核；真实验收只在约定窗口执行。Trace 写预计批次、候选 / 重检查次数、外审或 stage 窗口、明确的时间或费用上限，**并给量级预估（子代理人次、token 量级、PR 数、CI 次数、产品负责人触点次数——批准、亲跑、裁定），收口评论逐项对账。PR 数 = 批次数 + 须单独批准的 PR + 记录 PR；云端门禁 = PR 数 + 预计重跑；本机门禁 = 每个冻结候选 1 + 计分候选各 1 + 预留重跑；批次链固定成本按 2 轮修复包估；批次时间把外审与定向复核可并行的部分算进去，不按串行相加**；超出需说明。**真人验收是缺陷发现口，不只是取证动作**：候选预算须写明真人验收打回时重做几轮完整门禁与重新部署，收口对账写「因打回重做的轮次：预估 / 实际」。到线（含任一家周额度剩余 **≤ 2%**）时完成当前安全单元、保存证据、释放资源，再交接或等重置、请求预算或范围调整；剩余 2%–5% 照常派发。不能靠跳过失败或降低证据等级省预算。

**规划前先核本机能力并写进开工快照**：Claude Code 版本、会话模型与档位、剩余额度与重置时点；本 Trace 若启用外部审查工具，写明工具、模型、额度、怎么调用——**怎么调用这一条规划者必须实机测试一次并记录调用姿势与结果**，不得凭记忆或文档声称可用。外审频率按额度定：额度充裕时按批次甚至按候选频繁外审，额度紧张时只在最终 PR 前调用一次；频率与额度写进预算段，收口对账。

### 命中才展开的合同

- **共享资源：**只记录实际会冲突的数据库、机器人、端口、队列、令牌、stage、合并道或工作树；写唯一 Owner、取得条件、释放与清理。同一工作树同一时刻只有一个写入者；并行变异或重检查必须隔离目录和依赖；两条并行分支改到共同文件时，在独立工作树先做合并演练。
- **外部写与发布：**写目标、获批动作、固定制品、恢复方法和回读证据。审核对象、验收对象与发布对象必须对应；候选 SHA / digest 变化后，旧结论按影响失效。**候选冻结后产生的纯记录提交不推回该 PR**——推了 PR 头就离开被验收的候选，且按整体差异重跑完整门禁；合并后另走文档 PR，收口时 PR 头 SHA 须逐字等于验收单里的候选 SHA。**交给产品负责人在目标机执行的持久化动作**，做成幂等脚本 + 逐步回读 + 回退脚本，无交互、先在本机假根目录按目标机事实跑一遍；机密只走本人终端，不经代理会话。
- **真实验收：**只写本次断言、证据层级和实际观测入口。页面可见性与平台客观状态分别取证；测试替身、内部成功状态或绿色 CI 不能冒充真实用户结果。**每条判据写明被判的值由谁写入**：判定字段只认系统写出的值，前置状态允许人工构造；回答不了「这个值是谁写的」的判据无效。每条判据标注出处（日志、状态账或平台回读），请求方先按判据自跑一遍。真实环境用例逐步回源核对执行分支是否含不可逆操作，命中即改可逆路径或停止上报；「有还原步骤」不等于可逆，转抄核对核语义不核字面。
- **事实陈述的回源：**合同与工作项里「不应用的代价」「现在做不到什么」「某项为空」这类陈述必须回源到直接证据，不得由相邻状态推导：组件未启用推不出该类能力不存在，配置键存在推不出值为空或非空——登记写键与值的计数。写不出直接证据时如实写「未知」与取证方法。
- **身份 / 权限 / 数据形态类修法：**派实施前先对受影响的真实样本（生产或预发真实数据）做一次只读、脱敏的形态分布统计，把典型形态写进卡面与验收判据，产品裁定攒到开工前一次给齐；审核者核「代码符合裁定」，不替编排者验「裁定覆盖真实形态」。
- **首触冒烟与首次真机：**每个正式入口（脚本、命令、卡片、接口）在规划期或批次早期先在目标环境真机 dry 一次；没有注入口时并入第一条正式用例；模拟重放不算，只在验收窗口首跑的入口按计划外返工计。**新的主机侧组件第一次真机运行单列 Step「走通骨架」**（装上、空转一轮、读写各一次），排在批次早期、完整演练之前、产品负责人在线时段，按 3 倍估时，候选与亲跑次数各预留至少 2；三级审查审的是逻辑，不是主机现实。

## 四、编排方式

默认由一个顶层编排任务持续推进，独立且有边界的实施、调查或验收可交子代理。

### 通用编排纪律

**守望者的值守必须落成机制，机制本身要能被查。**守望者须同时挂两类机制，缺一即欠覆盖：**事件型**管「死没死」（常驻轮询，只用本地证据：窗口输出、工作树提交；查询失败须显式重试并声明）；**定时型**管「做到哪」（按周期给守望者一个完整回合，查外部留痕并对照任务表）。两类机制由合同授权，都是会话级，窗口关闭即失效，合同须写明失效后由谁接手；定时型自带收尾条件。守望者不得声称任何未被机制支撑的巡检频率，声称时给出机制标识与启动证据。**机制上线前，先对机制本身跑一遍下面的有效性判据。**

**兜底观察的有效性判据**（守望机制与任何外部等待的观察哨同样适用）：循环条件必须能区分「目标达成」与「查询本身失败」。观察脚本必须显式校验响应可解析，不可解析时继续重试，并覆盖成功、失败、超时与观察哨自身被终止四种结局。**「挂了观察哨」不等于「观察哨会叫醒我」**，判活只信外部证据。

**守望者汇报固定五项**（每次巡检、产品负责人问进度、每次触点前）：① 总体进度：当前阶段 + 已完成 Step / 总 Step 的百分比；② 预计剩余时长与预计完成时点（产品负责人所在时区，按本 Trace 实测速度估，每次巡检更新）；③ 当前堵点：卡在哪个对象、等谁、已等多久；④ 下一次需要人类决策或干预的预计时点与内容；⑤ 可提前给的裁定或授权：列出能预批的选项、默认值、有效期与不回复时的停止位置，让产品负责人一次给齐。用产品负责人能直接读的语言，不用内部代号；预测与实际的对账进批次收口。

### 并行纪律

可并行与否在立项时决定：规划者按文件 / 模块给每个实施 Step 划不重叠的边界，步骤表写明并行组，同一批次内实施 Step 默认并行、整合 Step 串行；估算给串行与并行两个时长；并行上限按机器内存、额度速率与编排者的回收能力写进开工快照。编排者执行时：① Ready 且不共享独占资源的 Step 同时派，不排队；每张派发卡写死独立工作树、独立临时目录与文件边界，越界即停。② 本机完整门禁、预发 / 生产窗口、合并道、共享外部通道一次一个，队列写进任务表「等待 / 占用」。③ 并行实施、串行整合：**实施卡只跑快检与受影响模块测试，不跑完整门禁**；多张卡完成后先冻结一个整合候选，完整门禁只在该候选上独占跑一次，再做一次审核。④ 先绿先合，后合的分支先并入主干现头再重跑门禁；两条分支碰同一文件属于规划失误，退回规划者拆开。⑤ 编排者只派发与回收、不亲自实施，每路并行都挂兜底观察。收口对账写计划并行度、实际并行度与因冲突返工的次数。

### 运行面

编排原语与角色流程由 `trace-kit` 插件承载（开工合同、派发卡、守望者、交接、接管、看板），**模板不重复其正文**；Trace 只写本次绑定的角色与授权终点。要点：子代理由主代理派发且不得再下派，完成时主代理收到通知；会话在项目主检出拉起，隔离工作树放主检出下的隔离目录，不放系统临时目录；跨会话接力用 tmux 拉起窗口与判活，**会话之间的转达一律走运行时的会话间消息**（有投递回执），向对方窗口注入按键不构成「已转达」，会话消息不是留痕；同一时刻只允许一个写入者。

### 派发、审核与验证

派发只需要六项：Step、可观察产物、范围、Owner / 工作区、通过证据、失败去向；**通过证据与禁令都写死（钉住用例名、变异项、提交正文要求；已裁定取消或禁止的动作逐条列入派发卡与接手指令），不留给执行者自觉，也不依赖中途转达**。**单个子代理任务预计超过 60 分钟即必须切分**；派发前给出预计时长，超时按兜底观察处置。授权内工作一路做完并等检查终态；需要产品取舍、扩大范围或新增外部副作用时停止。

每个固定候选由一名未参与实现的审核者审查；实施者修复，同一审核者定向复核；复修与定向复核优先续派原子代理，续派单独计次。**门禁、身份、权限类候选的审查卡写死威胁模型，审核者按模型复攻；同类绕过出现第二个变体时停止补丁，把「信任来源」作为设计裁定上提。**

**假证据六型（每个固定候选必查）**：①测试替身硬写被测判据，绕开真实规则表；②守卫按路径扫源码，实现搬走后扫的不是持有实现的文件；③变异锚点指向已删符号；④契约测试被条件跳过；⑤测试名、docstring 或验收断言里的无证据陈述（含「已实测」「已复核」类字样）；⑥断言与实现相反。**判据**：删掉被验证的那一行，测试仍全绿 ⇒ 该用例证明的是它自己注入的假设；断言总数不减少不构成覆盖面证据。候选变化后按影响重验。红色结果须保留并诊断，不能反复重跑到绿后丢弃；**门禁绿与变异红都以日志原文的实际运行数为准（Ran N，N > 0）**，环境错误导致的零用例红不计验红、收集错误的绿不计通过；门禁红先在未改动基点复现再修。**碰主机的候选另核真机语义清单**（权限位、属主、子进程副作用、外部资源存在性、平台属性）；测试替身没有同一输入的真调用对照，其用例不计「已测」。

本机检查只证明实际跑过的范围；与执行机系统或架构相关的检查转到 CI 或受控环境。**本机完整门禁运行期间不得有任何其他测试进程（含审核者的定向复核）；违反即视为无效运行，不计入证据。**回收子代理产出时，编排者以自己实跑的快检和主检出状态为准，不采信自报。

外部审查工具（本 Trace 启用时）的通道与调用姿势以开工快照里的实机测试为准（第三节）。**会合入仓库的产出（实现、修复、文档）一律由 Claude Code 子代理完成；外部审查工具只做只读核实、探针实跑与交叉验证**——交叉验证至少一家真跑，静态判断只作分歧信号不作结论；生产前走查保留一轮「绕过面」视角。正式审核必须能读取完整固定候选；台账先记输入载荷（候选 SHA、读入字节数或文件数）、模型与调用姿势，再记发现、证据、影响和未验证范围；退出码为零、报告非空不构成「已审」。通道小样只证明可调用，不证明仓库候选已通过。

## 五、状态、恢复与收口

Step 使用 `Pre-ready / Ready / Running / Waiting / Done`。编排者只派 Ready；合同缺项或范围、授权、验收标准变化时，停止受影响 Step 并退回规划者修订，不受影响的 Ready Step 可继续。

CI、远端命令、部署和队列必须回读实际对象的终态；处于等待时记录对象、观察入口和失败去向，不能因“已发起”判定完成。**门禁的耗时余量是可观察量**：每次完整门禁记录最长单步耗时与其超时上限之比，收口对账列出；余量低于两成先调上限再推下一轮，不等它撞线。时间记录使用系统实取的日期、时区和起止点，不把并行耗时相加。

`任务表.md` 顶部始终保留三行：

- **当前：**阶段、Step、固定候选和最后一条通过证据。
- **下一步：**唯一下一 Ready Step、Owner 和入口。
- **等待 / 占用：**阻塞条件、决定人，以及仍持有资源和清理方法；没有则写“无”。

跨任务接力时，继任者先读合同与恢复点并确认接管；旧编排者确认停止写入、释放租约后退场，不能出现两个写入者。**接力只发生在批次收口序列或失联判定，不因上下文余量中途交接；批次收口点默认换编排者，条件 = 当任上下文已用 ≥ 50%，低于可续任。**

**复盘规则**：

- 时机：编排者在每个批次收口序列内写，顺序固定为收口评论 → 复盘评论 → 交接评论 → 释放租约；Trace 收口时由末任编排者补全程终值；例外形态的编排者各写一份。复盘评论落地才算退场完成。规划者在 Trace 收口时写合同对账（估算与实际、范围改了几次、哪些事实本该开工前核出、哪些裁定本可开工前一次给齐），已转守望者的由守望者代写；守望者退场登记前写机制对账（失联与误报次数、转贴与触点等待、汇报预测与实际）。
- 编排者复盘内容六项：① 做对的（保留）；② 做错与可改进（各附修法）；③ 接缝与陷阱（本机 / 环境事实）；④ 墙钟按环节归类与排期对账；⑤ 用量按模型与通道；⑥ 方法候选，每条标归属（方法正文 / trace-kit 程序 / 仓库文档或工作项 / 本机记忆）与出处评论。
- 落点：复盘原文落 Trace Issue；方法候选由守望者在退场登记前汇总成一条归档评论落到本版修订 Issue（第八节），退场登记里给链接；接缝进本机记忆或仓库工作项；「方法已覆盖、执行没照做」的现象只留在复盘，不进正文。

批次收口只记录：达到的结果、候选与检查、剩余 / 下一步、待决定项、资源清理，以及**墙钟按环节归类**（有效机器工作 / 等产品负责人 / 等额度与换人 / 返工 / 浪费）、并行度与预算逐项对账。主链从 Step Owner、PR / commit / CI、审核记录和交接快照复盘，不重复逐命令日志。**记录 / 收口 PR 只改文档与变更日志；分支名与 PR 标题不得拼出平台的自动关闭关键字。**

最终 `Complete` 只表示全部获批范围达到唯一授权终点。`Partially Delivered / Stopped / Superseded` 须由产品负责人决定，并给每项未完成工作一个明确去向。谁创建临时进程、容器、工作树、测试实例或自动化，谁在收口前清理并回读；需要保留时写 Owner、原因、清理方法与时点。

## 六、Ready 自检

- [ ] 结果、范围、不做清单、唯一授权终点和批准版本明确。
- [ ] **里程碑分诊表已入开工快照，四项未齐的 Issue 没有被写成 Ready；实施 Step 已划并行组与文件边界；上一 Trace 的方法候选已结清（修订 Issue 已关或写明理由）。**
- [ ] 至少一个 Step Ready；其他 Step 的进入、成功和失败 / 等待去向明确。
- [ ] 人类 Gate 已前置或预授权；关键未知没有被写成 Ready。
- [ ] 命中的共享资源、外部写、候选、验收、审核与恢复可执行。
- [ ] **新写的启动条件、完成条件与清零判据已对照它所依赖的对象集合自检**：不自指、不互相等待；命中即显式写排除项。
- [ ] **开工快照写明执行机的时钟基准与时区、Claude Code 版本、会话模型与档位、额度与重置时点，以及（若启用）外部审查工具、模型、额度与实机调用测试记录**；面向产品负责人的时刻一律双标注且两个值都实读，跨夜任务核日期不只核时分；外审频率已按额度写进预算段。
- [ ] **碰主机的新组件已单列首次真机 Step 并按 3 倍估时；产品负责人触点次数已入预算。**
- [ ] 编排者从合同与任务表能回答：现在做什么、谁做、何时可做、怎样算过、失败去哪、何时必须停。

## 七、可复制的最小结构

`[tracking]` Issue 只放目标、文件链接和阶段。以下分别写入 `合同.md` 与 `任务表.md`；未命中的附加项不建空表。

```markdown
# Trace：一句话结果

## 给产品负责人
- 结果与授权终点：
- 范围 / 不做：
- 待决定或已预授权的 Gate：无 / G-…
- 方法：#678 v23；下一版修订 Issue：#…；Trace 版本、批准时间与证据：
- 输入、基线与完成标准链接：
- 触发：外部写=无；共享资源=无；跨任务=无；独立审核=无；特殊预算=无；首次真机=无

## 执行步骤
| ID | 产物 | Ready 条件 / Gate | Owner / 工作区 | 并行组 / 文件边界 | 通过证据 | 后继 / 失败去向 |
| --- | --- | --- | --- | --- | --- | --- |

## 命中才展开
- 外部写与恢复：
- 共享资源与释放：
- 独立审核 / 跨任务接力 / 特殊预算 / 首次真机：

# 任务表
- 当前：
- 下一步：
- 等待 / 占用：无
- [ ] S-… 状态｜一句话｜关键证据链接

# 收口
- 达到的终点与证据；未完成去向；清理结果；墙钟按环节归类与并行度对账：
```

## 八、版本与来源

**修订规则（v20 起）**：删除侧以「连续两批零引用，且由不受该条款影响的人复核」为判据；新增每条须点名一处删除并附事故或实测出处，产品负责人点名的新增可先净增、删除侧由下一版结清；「方法已覆盖、只是执行没照做」的现象只进 Trace 收口，不进正文。**每个版本一个修订 Issue**（v22 起）：立项时由规划者建下一版的修订 Issue，收各 Trace 的候选归档、草案、发布记录与同步清单；发布且同步完成才关闭，关不掉即未结清；本 Issue 只保留现行正文与发布指针。

- **v23，2026-09-20：**按 [#843](https://github.com/Moshuiwang/lingxi/issues/843)（2.5.2）两任编排者复盘经守望者归档到修订 Issue [#842](https://github.com/Moshuiwang/lingxi/issues/842)（候选 A1–A3）与产品负责人 2026-09-20 裁定（**模板只供 Claude Code 使用，删除全部其他执行者相关内容**）先删后增：删 / 并 5 处（执行者中立表述三处、「外部代理夜班实施形态」与「验证请求执行者身份一字确认」两条保持候选）、改 6 处（运行面标题、外审措辞四处、Ready 自检一项）、增 3 处（触点纪律进人类 Gate、实施卡不跑完整门禁进并行纪律 ③、批次链估法进预算）；逐条变更表见 #842。**继续保持候选**：「单个子代理超过 60 分钟切分」两批多次超线无事故，先不动。**删除侧欠账**：本版删除按产品负责人点名与明显冗余执行，未做 v22 全文「连续两批零引用」统计，由 v24 修订 Issue 结清。
- **v22，2026-09-18：**按 [#812](https://github.com/Moshuiwang/lingxi/issues/812)（2.5.1）三任编排者与夜班复盘、产品负责人两条裁定（收口点换人 ≥ 50%、额度线 2%）与 2026-09-18 点名的四项（规划者里程碑分诊、守望者汇报五项、编排者复盘规则、并行纪律）先删后增：删 / 并 4 处、改 8 处、增 6 处，角色表改 4 行，角色数不变；逐条变更表与候选归档见修订 Issue [#840](https://github.com/Moshuiwang/lingxi/issues/840)。
- **v21，2026-09-16：**按 [#770](https://github.com/Moshuiwang/lingxi/issues/770) 三批次复盘（四任编排者复盘经[守望者归档](https://github.com/Moshuiwang/lingxi/issues/678#issuecomment-5681280579)）与产品负责人两条裁定（[退场前必写复盘](https://github.com/Moshuiwang/lingxi/issues/770#issuecomment-5658442321)、[外包分档定稿](https://github.com/Moshuiwang/lingxi/issues/770#issuecomment-5663267212)）先删后增：删 / 并 10 处、改 / 增 17 处，再按产品负责人裁定精简冗余与解释句，逐条变更表见 v21 草案评论 [r1](https://github.com/Moshuiwang/lingxi/issues/678#issuecomment-5692852673) / [r2](https://github.com/Moshuiwang/lingxi/issues/678#issuecomment-5692941237)。
- **v20 / v20 r1，2026-09-13：**按 #709 / #732 / #754 三批复盘先删后增（[草案](https://github.com/Moshuiwang/lingxi/issues/678#issuecomment-5651545771) / [发布](https://github.com/Moshuiwang/lingxi/issues/678#issuecomment-5651559724)）；r1 追加规划前核本机能力与外审实机测试（[r1 发布](https://github.com/Moshuiwang/lingxi/issues/678#issuecomment-5653777117)）。
- **v19，2026-09-09：**模板不再绑定单一执行者；新增兜底观察判据、判据自指自检、代价陈述回源三条（净增，删除侧由 v20 结清）。
- **r1–r3，2026-09-08/09：**纳入 [#665](https://github.com/Moshuiwang/lingxi/issues/665) 已批准 1–8 项，本 Issue 升为唯一入口、#147 降为历史；按[第二轮精简审查](https://github.com/Moshuiwang/lingxi/issues/678#issuecomment-5588694204)净减 59%，再按[结果核对](https://github.com/Moshuiwang/lingxi/issues/678#issuecomment-5596074312)补回丢失候选。
- **v17 及更早版本：**见 [#147](https://github.com/Moshuiwang/lingxi/issues/147)。

Claude Code 能力以其运行时工具说明与官方文档为准；仓库验证以[验证与门禁](https://github.com/Moshuiwang/lingxi/blob/main/docs/技术设计/验证与门禁.md)及[本机检查说明](https://github.com/Moshuiwang/lingxi/blob/main/scripts/dev/README.md)为准。能力核验与一次性证据留在本 Issue 评论，不进入模板正文。
