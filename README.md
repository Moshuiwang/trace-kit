# trace-kit

把 lingxi 两个月实证有效的「工作方法资产」打包成可复用套件：执行方法正文（`METHOD.md`）、Claude Code 插件（`plugin/`）、新项目骨架（`template/`）、lingxi 特有实现示例（`examples/lingxi/`）。

> 建设中（Trace [#1](https://github.com/Moshuiwang/trace-kit/issues/1)）。v0.1.0 发布后本 README 换成安装两步与目录导览。
